双端智能教育平台架构白皮书：以计算思维课程为例的通用系统设计
1. 执行摘要：从内容托管到知识编排的范式转移
随着大语言模型（LLM）与检索增强生成（RAG）技术的成熟，教育技术领域正经历一场从静态资源托管向动态知识编排的深刻变革。传统的学习管理系统（LMS）往往仅作为数字化档案柜存在，缺乏对教学过程的深度介入与智能化辅助。本报告旨在阐述一种全新的双端智能教育平台架构，该架构通过明确区分并深度定制“教师端（Teacher Studio）”与“学生端（Student Learner）”的功能逻辑，实现了教学生产力的释放与个性化学习的规模化落地。
2. 理论框架：计算思维的认知模型与系统映射
在设计通用教育系统时，首要任务是确立领域知识的元模型。对于“计算思维”这一不涉及代码编写的课程，其核心在于培养学生处理复杂问题的思维习惯。系统设计必须将这些抽象的认知过程映射为可量化、可交互的功能模块。
2.1 领域定义的四个支柱
本系统将计算思维解构为四个核心支柱，这不仅是教学大纲的分类依据，更是底层RAG系统的索引逻辑与标签体系：
1. 分解（Decomposition）： 将复杂问题拆解为可管理的小模块。在系统层面，这映射为“分层文档处理技术”，即将长篇教材自动切分为具有父子关系的语义块（Chunks），并在前端以层级大纲的形式呈现。
2. 模式识别（Pattern Recognition）： 识别不同问题间的共同趋势。系统通过向量相似度搜索（Vector Similarity Search）来实现这一点，能够跨文档自动关联相似案例（例如，将生物学中的分类法与图书馆的图书索引法自动关联推荐）。
3. 抽象（Abstraction）： 剥离非必要细节，聚焦核心属性。这在功能上体现为“智能摘要”与“音频概览（Audio Overview）”模块，AI需具备从冗长文本中提取骨架的能力。
4. 算法设计（Algorithm Design）： 制定解决问题的分步流程。这是“苏格拉底式辅导系统（Socratic Tutor）”的核心逻辑，要求AI引导学生一步步构建逻辑链条，而非直接给出答案 (1)。
2.2 非代码化评估的系统约束
3. 教师端架构（Teacher Studio）：知识编排与生成工场
教师端（Teacher Studio）的设计哲学是“杠杆效应”——最大化教师单位时间的产出价值。它不应仅仅是一个文件上传界面，而应是一个集成了深度研究、内容生成与数据分析的智能指挥中心。
3.1 深度数据摄取与知识库构建
知识库是整个系统的基石。传统的LMS仅支持文件的简单存储，而本系统实施的是“深度摄取（Deep Ingestion）”。
3.1.1 结构化解析与混合索引
针对计算思维类教材通常包含大量图表、流程图与层级标题的特点，系统采用基于视觉模型（如Mineru或Unstructured）的解析管道 (1)。
- 层级感知切片（Hierarchy-Aware Chunking）： 解析器会识别Markdown标题（# H1, ## H2），确保知识切片不会打断语义的连续性。例如，关于“递归”定义的段落会与其下的“汉诺塔示例”保持父子关联，而非被生硬地切分到两个不同的检索块中。
- 多模态增强： 对于教材中的流程图，系统利用视觉多模态模型（VLM）生成详细的文本描述（Alt-Text），使这些非文本信息也能被RAG检索引擎捕获。
3.1.2 深度研究智能体（Deep Research Agent）
为了解决教材内容滞后的问题，教师端集成了基于LangGraph框架的“深度研究智能体” (5)。教师只需输入指令：“搜集关于生活中算法偏见的最新案例，用于解释‘模式识别’的负面影响”，智能体便会执行以下工作流：
1. 规划（Planning）： 将任务拆解为搜索关键词（如“algorithmic bias real world examples 2024”）。
2. 执行（Execution）： 调用Tavily或Google Search API进行广度搜索，并过滤掉非权威来源（如社交媒体、内容农场）。
3. 清洗与合成（Scraping & Synthesis）： 使用Firecrawl等工具抓取网页正文，去除广告噪音，并将多源信息合成为一篇结构清晰的Markdown简报 (7)。
4. 入库（Ingestion）： 教师审核通过后，该简报被自动加入课程知识库，成为后续生成考题或AI辅导的合法依据。这一过程从根本上区别于简单的网页爬虫，它具备了人类助教的筛选与总结能力。
3.2 生成式工场：从文本到多模态教学制品
这是系统最显著区别于传统LMS的模块，深受NotebookLM启发，旨在将静态文本转化为动态教学资源。
3.2.1 播客级音频概览生成（Audio Overview Generator）
本模块利用大模型与语音合成技术（TTS），将枯燥的教材转化为生动的双人对谈节目。这对于听觉型学习者以及利用碎片时间学习至关重要 (8)。
- 角色工程（Persona Engineering）： 系统预设了两个AI主持人角色——Host A（专家型，理性、学术）与Host B（好奇型，感性、善于类比）。针对“计算思维”，Host A负责解释“抽象”的定义，Host B则会打断说：“等等，这听起来像是地铁线路图？忽略了具体的街道弯曲，只保留了站点连接？”这种对话设计极大地降低了认知门槛 (10)。
- 生成管道（Pipeline）：
  1. 源文本提取： RAG引擎提取本周课程的核心章节。
  2. 脚本撰写（Scripting Agent）： 专用LLM生成JSON格式的对话脚本，包含情感标注（如[laugh], [thoughtful pause]）和互动逻辑。
  3. 音频合成（TTS Synthesis）： 调用高质量TTS模型（如ElevenLabs或OpenAI Audio），支持多说话人与情感控制，生成MP3文件 (11)。
- 教学价值： 教师无需录音设备，仅需一键即可为每一章教材生成配套的“导读播客”，极大地丰富了课程资源的形态。
3.2.2 干扰项感知的考题生成器（Distractor-Aware Assessment Generator）
针对“无代码”的限制，考题质量直接决定了评估的有效性。系统摒弃了简单的“生成5道选择题”指令，而是采用了基于**误解分析（Misconception Analysis）**的生成策略 (3)。
- 生成逻辑： 当教师要求生成关于“算法效率”的题目时，系统会首先检索“初学者常犯错误库”。
- 干扰项设计：
  - 正确选项： 快速排序在平均情况下比冒泡排序更高效。
  - 干扰项A（基于直觉错误）： 冒泡排序代码更短，所以效率更高。（测试学生是否混淆了代码长度与时间复杂度）。
  - 干扰项B（基于概念混淆）： 快速排序需要更多内存，所以速度更慢。（测试学生是否混淆了空间复杂度与时间复杂度）。
- 解析生成： 系统不仅生成答案，还为每个干扰项生成解释：“如果你选了A，说明你混淆了实现难度与运行效率……”这种反馈对学生自学至关重要。
3.3 全局洞察仪表盘（Insight Dashboard）
仪表盘的设计不仅展示分数，更通过**知识图谱热力图（Knowledge Graph Heatmap）**展示班级的认知状态 (13)。
- 可视化逻辑： 课程的所有概念（如分解、循环、条件判断）构成一个图谱网络。节点颜色代表班级平均掌握度（红=低，绿=高）。
- 关联分析： 教师可能发现“抽象”节点是绿色的，但与其相连的“建模”节点是红色的。这提示教师：学生理解了定义，但无法将其应用于实际建模。
- 行动建议： 系统会自动推荐干预措施：“建议为‘建模’概念生成一个新的音频概览，或发布一组针对性的情境判断题。”
4. 学生端架构（Student Learner）：自适应探索与苏格拉底式交互
学生端的设计核心是“引导”而非“灌输”。界面去除了复杂的配置选项，保留了极简的学习路径与深度的交互窗口。
4.1 苏格拉底式智能导师（Socratic AI Tutor）
这是学生端的核心交互组件，也是实现“个性化教学”的关键。不同于ChatGPT的通用模式，该导师被严格限制了行为边界 (2)。
4.1.1 产婆术（Maieutics）提示工程
系统通过包含Few-Shot Examples的System Prompt（系统提示词），强制AI执行苏格拉底式的提问策略。
- 禁止直接回答： 当学生问：“什么是贪心算法？”时，AI绝不会直接输出维基百科式的定义。
- 引导式反问： AI会回答：“在回答之前，想象一下你在自助餐厅取餐，想在盘子装满前拿到热量最高的食物，你会怎么选？是每次都拿眼前看起来热量最高的，还是先看一遍所有菜？”
- 思维脚手架（Scaffolding）： 如果学生连续三次回答错误，AI会动态降低问题的抽象层级，提供更具体的线索，直到学生自己悟出“局部最优解”的概念 (17)。
4.1.2 基于源文件的溯源交互
为了培养严谨的学术习惯，导师的所有回答都必须基于课程资料。
- 引用标注： AI的回复中会自动嵌入引用角标（如[教材P23]）。
- 点击展开： 学生点击角标，侧边栏会滑出PDF原文的对应段落，并高亮显示。这种双栏对照设计（左侧对话，右侧原文）有效地解决了AI幻觉问题，并强化了证据意识 (1)。
4.2 沉浸式音频学习体验
学生端的音频播放器并非简单的MP3播放器，而是一个富媒体学习工具。
- 动态脚本同步： 类似于Apple Music的歌词滚动，音频播放时，系统生成的对话脚本会实时高亮滚动。
- “那是谁/那是什么”功能： 学生在听到陌生术语（如“图灵测试”）时，可以点击脚本中的词汇。此时音频自动暂停，苏格拉底导师会弹出一个轻量级解释卡片，甚至引导学生进行简短的问答，确认理解后再继续播放。这种**主动式中断（Active Interruption）**将被动听讲转化为主动探究。
4.3 动态学习路径导航（Adaptive Navigator）
系统不再展示线性的章节列表，而是根据学生的认知状态（User State）动态调整推荐内容。
- 状态追踪： 系统记录学生在每个知识点上的“置信度”。如果学生在“模式识别”的测验中表现不佳，系统会锁定下一章“算法设计”的进入权限。
- 补救路径： 系统会自动生成一个“补救包（Remedial Pack）”，包含针对该知识点的3分钟音频切片、5张核心概念闪卡（Flashcards）以及2个简易的情境判断题。只有完成补救包，主线课程才会解锁。
5. 通用性与扩展设计：以计算思维为原点
虽然本报告以计算思维为例，但该架构的精髓在于其内容无关性（Content-Agnostic）。系统对“计算思维”的处理逻辑——即将其视为概念网络而非代码库——使得该平台可以无缝迁移至其他学科。
5.1 跨学科迁移机制
- 历史课迁移： “分解”变为“历史分期”，“模式识别”变为“历史周期律分析”。系统只需替换底层的知识库与误解库，苏格拉底导师的提问逻辑（引导分析而非罗列年代）依然适用。
- 文学课迁移： “抽象”变为“主题提取”，“算法设计”变为“叙事结构分析”。音频概览可以生成关于《红楼梦》人物关系的深度对谈，而非算法逻辑的讨论。
5.2 知识图谱的动态重构
系统的核心资产并非具体的内容，而是知识图谱的构建模版。
- 教师在使用初期，可以通过自然语言描述课程结构：“这门课分为四个模块，模块之间存在递进关系。”
- 系统利用LLM自动构建初始图谱，并随着文档的上传不断细化节点间的边（Edges）。这种“元数据驱动（Metadata-Driven）”的设计保证了系统的高度通用性。
6. 技术架构实施细节
为了实现上述功能，系统采用微服务架构，强调高并发、数据隔离与模块化编排。
6.1 技术栈选型
- 后端核心： Python (FastAPI)。选择Python是因为其在AI生态中的统治地位，便于集成LangChain、LlamaIndex等库 (1)。
- 前端框架： React + TypeScript。采用组件化设计，便于复用“音频播放器”、“聊天窗口”等UI组件。教师端使用Ant Design Pro构建复杂后台，学生端使用Tailwind CSS构建移动端友好的响应式界面。
- 向量数据库： Milvus或ChromaDB。支持多租户（Multi-Tenancy）是关键，必须确保不同课程、不同学校的数据在物理或逻辑上严格隔离 (20)。
- 编排框架： LangGraph。用于管理复杂的Agent工作流（如播客生成、深度研究），支持循环（Cyclic）图结构，允许智能体在任务未完成时自我修正与重试 (5)。
6.2 关键数据流设计
1. 上传与解析流： PDF -> Mineru解析 -> 语义分块 -> 向量化 -> 存入向量库。
2. RAG检索流： 学生提问 -> 查询重写（Query Rewriting） -> 混合检索（向量+关键词） -> 知识图谱增强 -> LLM生成 -> 引用注入 -> 返回前端。
3. 音频生成流： 选定文档 -> 剧本生成Agent -> 剧本审核Agent -> TTS引擎 -> 音频混流（加入背景音） -> 存储至S3/OSS -> 生成时间戳索引。
6.3 安全与隐私
- PII屏蔽： 在将数据发送给外部模型（如OpenAI/Anthropic）前，通过中间件层自动识别并替换学生姓名、学号等敏感信息（PII Redaction）。
- 本地化部署选项： 架构支持将推理层切换为本地部署的开源模型（如Llama 3或Mistral），以满足对数据主权有极端要求的机构 (23)。
7. 结论与展望
本报告提出的双端智能教育平台，通过将“计算思维”这一抽象学科作为切入点，展示了AI在非代码类深度教学中的巨大潜力。其核心创新在于：
1. 角色的重新定义： 教师从“出题机器”变为“知识策展人”，学生从“被动接收者”变为“主动探究者”。
2. 内容的动态重塑： 静态教材被实时转化为音频、对话与个性化考题，极大地提升了信息的利用率与可达性。
3. 评估的范式转移： 证明了即使不写一行代码，也能通过逻辑与情境的交互，精准评估学生的思维模型。
随着多模态大模型的进一步发展，未来的平台将不仅限于文本与音频，更将引入动态生成的视频概览与交互式概念图，最终实现真正的“因材施教”。这不仅是一个系统的设计文档，更是教育数字化转型的战略蓝图。

---
详细功能规范与设计模式
核心架构：分层知识库与内容流
为了支持“计算思维”的学习特性以及“教师引导-学生自主”的混合模式，系统引入了 “继承与扩展（Inheritance & Extension）” 的知识库架构。
2.1 “主知识库”与“个人层”的叠加模型
- L1：教师主知识库 (Master Knowledge Base)
  - 定义：由教师端维护的权威资料集，包含教材、算法导论 PDF、标准代码库、API 文档等。
  - 权限：教师拥有完全读写权；学生拥有只读权限（Read-Only）。
  - 作用：作为 RAG（检索增强生成）的基准来源，确保所有回答的底层逻辑符合教学大纲。
- L2：学生个人层 (Personal Layer)
  - 定义：学生在主知识库之上建立的私有空间。
  - 功能：学生可以上传自己的笔记、参考论文、调试过的代码片段。
  - RAG 逻辑：学生端的 AI 在回答问题时，会同时检索 L1 + L2 的内容，实现“权威知识 + 个人见解”的融合。
2.2 上下游内容发布流 (Content Publishing Pipeline)
系统不再是孤立的工具，而是内容流动的管道。
- 上游（教师）：利用 AI 辅助生成技术教学博客（如“递归算法详解”），经人工审核后**“发布”**至班级流。
- 下游（学生）：在学生端首页可以看到教师发布的博客，并能直接基于该博客内容向 AI 提问或进行代码实验。
1. 教师端（Teacher Studio）：知识编排中心
教师端是整个生态系统的“大脑”，其设计目标是赋予教师对AI的高度控制权，同时通过自动化工具极大降低备课负担。
1.1 课程与文档管理系统 (Deep Ingestion)
传统的LMS只是文件的堆砌，而本系统的文档管理是构建“课程大脑”的过程。
1.1.1 智能解析与分块 (Semantic Chunking)
对于计算思维课程，教材往往包含复杂的定义、伪代码和图表。系统必须采用高级解析技术：
- 技术实现： 集成 Mineru 引擎。这些工具能够识别PDF中的多栏排版、页眉页脚（并去除之），并将数学公式转换为LaTeX格式，将表格还原为Markdown表格 (1)。
- 分块策略： 摒弃固定字符数（如500字）切分，采用基于文档结构的切分（MarkdownHeaderSplitter）。这意味着一个关于“循环不变量”的完整小节会被作为一个独立的语义单元存储。
- 元数据注入： 每个切片在入库时会自动打上标签：{ "chapter": "3", "topic": "Loop", "type": "definition", "difficulty": "medium" }。这使得后续的检索可以进行精确的元数据过滤（Metadata Filtering）(24)。
1.1.2 课程知识图谱可视化
系统会根据上传的文档自动构建知识图谱。
- 界面设计： 教师可以看到一个可视化的节点网络。节点代表核心概念（如“抽象”、“递归”），边代表关联（“依赖于”、“是...的例子”）。
- 人工干预： 教师可以手动调整图谱。例如，如果AI错误地将“堆栈（Stack）”与“堆（Heap）”混淆，教师可以手动断开连接。这种**人机回环（Human-in-the-loop）**机制确保了知识库的准确性。
1.2 生成式工场 (Generative Workshop)
这是教师端最高频使用的模块，用于生产教学制品。
1.2.1 音频概览生成器 (NotebookLM-Style Podcast)
此功能的技术实现极其复杂，旨在复刻并超越NotebookLM的体验 (8)。
- 工作流架构：
  1. 源选择： 教师勾选“第三章：算法复杂度”和一篇补充阅读材料。
  2. 大纲规划（Planner Agent）： AI首先阅读材料，生成对话大纲。例如：“1. 开场白（轻松幽默）；2. 核心概念：大O表示法（用收拾房间做比喻）；3. 难点辨析：时间与空间的权衡；4. 结语。”
  3. 脚本生成（Script Writer）： 生成具体的逐字稿。系统强制要求包含非语言线索，如 (Host A 叹气) 或 (Host B 抢话)，以增加真实感。
  4. 音频合成（TTS Engine）： 使用支持SSML（语音合成标记语言）的高级TTS模型。系统支持OpenAI TTS HD 或 ElevenLabs，能够准确表现语调的抑扬顿挫 (11)。
  5. 后期混音： 自动添加淡入淡出的背景音乐（Lo-Fi beats），在人声对话时自动压低音量（Ducking）。
1.2.2 深度研究智能体 (Deep Research Agent)
当教材内容不足时，教师启动此智能体进行互联网内容的补充 (5)。
- 任务配置： 教师输入：“查找关于2024年巴黎奥运会中使用的算法技术，特别是关于人群管理的部分。”
- 执行逻辑：
  - 智能体首先在Google Scholar和TechCrunch等白名单站点进行搜索。
  - 它会阅读前10个结果的摘要，筛选出不仅包含技术关键词，还包含具体应用案例的文章。
  - 它将筛选出的3篇文章全文抓取，并生成一份“综合简报”，包含“人群模拟算法”、“计算机视觉安全监控”等知识点。
  - 教师点击“确认添加”，这些新知识即刻进入RAG系统，学生下一秒即可就此提问。
1.2.3 智能考题与干扰项生成 (Smart Assessment)
针对“计算思维”的非代码评估，系统专注于生成高质量的情境判断题 (3)。
- 生成Prompt设计： “请针对‘分解’这一概念生成一道选择题。场景设定为‘组织一次学校春游’。正确答案应体现将大任务拆解为小任务。生成3个干扰项，分别体现：1. 仅列出待办清单（混淆分解与列表）；2. 寻找去年的春游方案（混淆分解与模式识别）；3. 忽略细节只看大局（混淆分解与抽象）。并为每个选项提供详细解析。”
- 审题机制： 系统内置一个“批评者（Critic）”模型，用于检查生成的题目是否存在逻辑漏洞或答案不唯一的情况，确保题目质量。
1.3 学情分析仪表盘 (Analytics Dashboard)
不再关注“谁登录了系统”，而是关注“谁掌握了概念”。
- 概念热力图： 基于学生的答题和提问数据，实时更新全班对每个知识点的掌握度。红色预警区域（如“递归”）提示教师需要在下节课重点讲解 (13)。
- 交互深度分析： 统计学生在音频概览中的暂停点。如果全班有40%的学生在音频的03:45处暂停并点击了“解释一下”，说明该处的知识点（可能是某个晦涩的类比）存在普遍理解障碍。
2. 学生端（Student Learner）：沉浸式学习终端
学生端的设计理念是“极简主义”与“深度交互”的结合，支持移动端访问，适应碎片化学习场景。
2.1 智能学习路径 (Adaptive Learning Path)
- 动态流（Dynamic Feed）： 首页不是静态的目录，而是一个基于状态的动态流。如果学生刚完成第二章，首页首屏会显示：“准备好挑战第三章了吗？建议先听一下这段5分钟的导读。”
- 前置知识检查： 在进入新模块前，系统会隐式检查前置知识。如果学生在“逻辑门”测试中挂科，系统会锁定“电路设计”模块，并推荐“逻辑门复习胶囊”。
2.2 苏格拉底式辅导界面 (Socratic Interface)
这是学生最常驻的界面，UI设计类似于微信或ChatGPT，但功能完全不同。
- 引导式对话：
  - 学生： “我不懂这道关于抽象的题为什么选C。”
  - AI： “我们先不看选项。你能用自己的话说一下，地铁图和真实的卫星地图有什么区别吗？”（引导学生思考抽象的本质——去粗取精）。
  - 学生： “地铁图没那么详细，只画了站点。”
  - AI： “正是！它忽略了真实的距离和弯曲，只保留了连接关系。这正是‘抽象’的过程。现在回头看选项C，它描述了什么？”
- 多轮次记忆： 系统通过LangGraph的checkpointer功能维持长短期记忆。即使学生关闭窗口第二天再来，AI还记得他昨天在“抽象”这个概念上卡住了，并会主动问候：“昨天关于地铁图的例子你理解了吗？” (1)。
2.3 音频概览播放器 (Interactive Audio Player)
- UI布局： 底部是播放控制条，主体区域是滚动的脚本文本。
- 深度链接（Deep Linking）： 脚本中的关键术语（如“算法复杂度”）显示为蓝色超链接。点击后，音频暂停，屏幕右侧滑出该术语在教科书中的原始定义和相关图表。这种设计实现了听觉流与视觉流的无缝切换 (25)。
- 倍速与分段： 支持0.75x到2.0x倍速，且音频被AI自动打点分段（如“00:00 介绍”、“03:20 核心概念”、“08:15 案例分析”），方便跳转。
2.4 游戏化自测 (Gamified Self-Assessment)
- 每日一题（Daily Challenge）： 推送一道基于学生薄弱点的逻辑题。
- 连胜激励（Streaks）： 鼓励持续学习。
- 闪卡模式（Flashcards）： 利用间隔重复算法（Spaced Repetition System, SRS）自动安排复习。卡片正面是场景（“寻找图书馆的一本书”），背面是对应的思维模式（“二分查找”）。
3. 核心技术架构与实施方案
3.1 系统架构图谱
系统采用典型的RAG 2.0 架构，分为数据层、编排层与应用层。
3.1.1 数据层 (Data Layer)
- 向量数据库 (Vector Store): 推荐使用 Milvus 或 Qdrant。需配置为多租户模式，通过 Partition Key 隔离不同学校的数据。
- 关系型数据库 (PostgreSQL): 存储用户账户、学习进度、知识图谱的节点关系数据。
- 对象存储 (S3/MinIO): 存储原始PDF文件、生成的MP3音频文件。
3.1.2 编排层 (Orchestration Layer)
- LangGraph: 作为核心的大脑。
  - 理由： 传统的LangChain是DAG（有向无环图），适合线性任务。但苏格拉底教学是循环的（提问-回答-评估-再提问），Deep Research也是循环的（搜索-评估-再搜索）。LangGraph的状态机（StateGraph）机制完美契合这些需求 (1)。
- 模型路由 (Model Router):
  - 简单任务（如总结、关键词提取）路由至 GPT-4o-mini 或 Claude 3 Haiku（成本低、速度快）。
  - 复杂任务（如苏格拉底引导、干扰项逻辑生成）路由至 GPT-4o 或 Claude 3.5 Sonnet（逻辑强）。
3.1.3 接口层 (Interface Layer)
- API Gateway (FastAPI): 处理所有的前端请求，管理WebSocket连接（用于流式输出对话）。
- 前端框架:
  - 教师端：React + Ant Design Pro。强调信息密度与管理效率。
  - 学生端：React + Tailwind CSS (Mobile First)。强调交互流畅性与沉浸感。
3.2 关键算法实现细节
3.2.1 混合检索策略 (Hybrid Search)
为了确保RAG的准确性，系统采用**BM25（关键词匹配） + Embedding（向量相似度） + Knowledge Graph（图谱关联）**的三路召回策略 (26)。
- 当学生问“书上的那个关于做饭的例子”时：
  - BM25能匹配到包含“做饭”关键词的段落。
  - Embedding能匹配到语义相关的“烹饪”、“食谱”段落。
  - Graph能关联到该例子所属的“分解”概念节点。
  - Rerank（重排序）： 使用 Cohere Rerank 或 BGE-Reranker 模型对三路召回的结果进行二次打分，选出最相关的内容送入LLM。
3.2.2 音频生成的提示词工程 (Prompt Engineering for Audio)
生成逼真对话的关键在于提示词的精细度。
System Prompt: 你是两位专业的教育播客主持人。
Role A (Alex): 资深计算机教授，严谨，喜欢用定义，但有时会陷入术语堆砌。
Role B (Jamie): 聪明但非专业的学生，总是试图用生活中的类比来理解A的话，会打断A，会发出惊叹。
Task: 基于提供的教材文本，生成一段5分钟的对话脚本。
Requirements:
1. 必须使用 JSON 格式输出，包含 "speaker", "text", "emotion" 字段。
2. Jamie 必须至少提出两个澄清性问题（"你的意思是...?"）。
3. 必须包含至少一个幽默的类比。
4. 在解释复杂概念时，两人的对话必须一来一回，不能有超过3句话的长独白。
3.3 数据安全与隐私保护
- 去标识化 (Anonymization): 在Prompt发送给LLM之前，通过正则或NLP实体识别，将学生姓名替换为 Student_ID_123，返回后再替换回来。
- 私有化部署: 架构设计允许将LLM推理层替换为本地部署的 Llama 3 或 Qwen 2.5，配合 vLLM 推理加速框架，实现数据不出校门的完全合规 (23)。
4. 总结
本“双端智能教育平台”设计方案，通过精细的架构设计，成功将“计算思维”这一抽象学科转化为可感知、可交互、可评估的数字化体验。
- 对教师： 它通过Deep Research和自动化生成工具，赋予了教师“超能力”，使其能以极低成本生产高质量的多模态课程。
- 对学生： 它通过苏格拉底式AI和自适应路径，提供了千人千面的辅导体验，将被动学习转化为主动构建。
这一系统的核心不在于取代教师，而在于通过技术接管“知识传递”的低效环节，让教师回归“育人”与“启迪”的高价值高地。随着技术的迭代，该平台有望成为未来混合式教学（Blended Learning）的基础设施标准。