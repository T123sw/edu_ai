use_default_settings: true

general:
  instance_name: "EduAgent-SearxNG"

server:
  port: 8080
  bind_address: "0.0.0.0"
  public_instance: false
  limiter: false
  secret_key: "eduagent-local-searxng-secret-2026-01-23"
  formats:
    - html
    - json

# 尝试禁用bot detection（如果支持）
ui:
  infinite_scroll: false
