# 已生成资料的 Agent 修改：验收文档（Agent C）

状态：C 模块已实现，分项验证完成；共享 UI 初始化竞态未通过，整体验收未完成。设计：[C 设计](../specs/2026-09-07-artifact-revision-design-cn.md)；计划：[C 计划](../plans/2026-09-07-artifact-revision-plan-cn.md)。

## 数据准备

在专用课程中准备两份不同的数组报告、一个链表报告、已发布资料、学生个人资料、无权访问资料，以及每种可用生成类型样例。原文加入可验证的独特段落，修改前保存基准版本和内容摘要，以证明实际读取并保留非目标内容。

## 验收矩阵

| ID | 操作 | 预期 | 状态 |
| --- | --- | --- | --- |
| C01 | 工厂结果／资源预览点击让 AI 修改 | 选中正确 ID 和版本、显示标题；未提交前无任务 | 未通过：跨页初始化可清空引用 |
| C02 | 提交“增加两个实际案例” | 读取原文，增加案例，非目标独特段落保留，新版真实保存 | 服务及真实模型通过；完整 UI 待验证 |
| C03 | 直接说修改上次的数组报告 | 唯一匹配时修改正确报告，不选其他课程最新报告 | 服务/共享 reply 测试通过；真实自然语言 UI 待验证 |
| C04 | 两份报告均可能匹配 | 追问选择；选择前无写入 | 服务测试通过；完整 UI 待验证 |
| C05 | 说“不太好”，再补充“简化表达” | 保留资料和原问题，补充后执行同一修改任务 | 服务测试通过；完整 UI 待验证 |
| C06 | 说“第二部分不对”且意图不可确定 | 先读原文再具体追问，不任意重写全文 | 原文读取后追问测试通过 |
| C07 | 关闭引用后普通提问 | 不继续默认修改旧资料 | intent/单次 UI 测试通过；完整回归未通过 |
| C08 | 无权限／资料已删除／原文读取失败 | 不泄漏正文、不新建冒充原文、不声称修改成功 | 服务测试通过（含服务端文件读取） |
| C09 | 保存失败／模型超时／非法结构输出 | 原版本及发布版本完好，可携原意见重试 | 服务测试通过；网络超时 UI 待验证 |
| C10 | 两窗口修改同一基准版本 | 后完成者检测冲突，不静默覆盖 | JSON 多进程、SQL CAS 测试通过；真实 PostgreSQL 待验证 |
| C11 | 同一提交重试或重复完成事件 | 不重复增加版本，结果一致 | 服务幂等通过；HTTP 网络重试待验证 |
| C12 | 查看修改／旧版／恢复 | 摘要对应真实变化，旧版可读，恢复可追踪 | 历史/恢复服务通过；历史 UI/API 接入待完成 |
| C13 | 修改已发布资料 | 新版为草稿；学生发布视图、任务快照及旧学习证据不变 | 发布副本不变测试通过；任务/学生全链路待验证 |
| C14 | 学生修改本人资料／他人资料 | 本人按权限成功，他人拒绝，不获得发布权限 | owner 隔离通过；学生主入口待验证 |
| C15 | B 当前未选知识点但已指定有效报告 | 修改按原资料范围进行，不重复问知识点 | 原引用课程/知识点优先测试通过 |
| C16 | 流式／非流式＋自然语言／按钮入口 | 四种组合使用同一定位、读取、澄清及保存规则 | 真实 ReplyServiceV2 四组合测试通过（模型替身）；HTTP 端到端待验证 |
| C17 | 修改进行中切换页面／课程 | 新版本归属原目标，不污染新范围 | 原目标归属服务测试通过；浏览器运行中切换待验证 |

## 各类型覆盖（逐项填写，不允许用“全部支持”替代）

| 类型 | 原文读取 | 意图修改／校验 | 新版保存／回退 | 预览／使用 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 报告／大纲 | JSON/SQL/旧 Markdown 文件通过 | 精确替换、大纲章节通过 | 新版/恢复通过 | 报告真实模型 v2；完整预览待验证 | 模块通过，UI 待验证 |
| 教案 | Markdown＋实际生成结构通过 | objectives/process 校验通过 | JSON/SQL 新版通过 | 预览/导出未完整验证 | 模块通过 |
| 博客 | Markdown 通过 | 局部修改保留媒体文本测试通过 | JSON/SQL 新版通过 | 预览待验证 | 模块通过 |
| 习题 | questions 通过 | 题干/选项/答案/解析结构检查通过 | JSON/SQL 新版通过 | 真实模型改题干保存 v2；作答评分 UI 待验证 | 模块及真实模型通过 |
| 闪卡 | cards 通过 | 正反面、增删保留顺序测试通过 | JSON/SQL 新版通过 | 翻卡 UI 待验证 | 模块通过 |
| 思维导图 | root 树通过 | 全局 ID 唯一/悬空引用检查通过 | JSON/SQL 新版通过 | 导图 UI 待验证 | 模块通过 |
| 游戏 | game_data 通过 | 三种现有模板 schema/配对分类验证通过 | 新版 HTML 独立路径生成通过 | HTML 生成验证；实际交互待验证 | 模块通过 |
| AI 课堂 | Stage/Scene/Action 通过 | 复用 validate_stage，旧配音失效测试通过 | JSON/SQL 新版通过 | 播放/指定版本导出待验证 | 模块通过 |

实际目录还有其他类型时追加。习题核对答案／解析与作答评分；游戏核对交互；课堂核对播放和指定版本导出。未支持项必须给出原因和后续工作，不能算完整交付。

## 证据要求与通过标准

- C02 记录输入、目标 ID／版本、读取证据、修改前后相关片段及保存记录；不能只凭模型说“已修改”。
- C04／C05 记录完整多轮与工具写入次数；C10／C11 用确定性存储测试证明冲突和幂等。
- 至少真实模型走报告完整链路及一个结构化类型；其余类型也要有真实适配、持久化和可用性验证。外部环境不可用则标明待验证，不能伪造通过。
- 入口 1366px／390px、键盘操作和错误反馈可用；回归原手动编辑、资源预览和导出。

## 执行记录

执行日期：2026-09-07。Python：`/home/zxqs_ep/miniforge3/envs/edu-ai/bin/python`；Node/pnpm：同一环境 bin。课程样本全部在临时目录；SQL repository 使用独立 SQLite 内存数据库，不连接真实资料库。无数据库 DDL 迁移，复用 Material/MaterialVersion；JSON manifest 增量保存 revision_history/revision_operations。

| 检查 | 结果 | 证据路径 | 未解决问题 |
| --- | --- | --- | --- |
| 模块/存储/共享 reply 回归 | 见 pytest.txt 最终结果 | [pytest](evidence/2026-09-07-artifact-revision/pytest.txt) | SQL 方言在 SQLite 测试，真实 PostgreSQL 待验证 |
| 前端 intent/各工厂 scope 请求 | 10 passed | [前端测试](evidence/2026-09-07-artifact-revision/frontend-tests.txt) | — |
| 浏览器主页面＋mock SSE | 单次 1366px 曾通过；最终双尺寸 2 failed | [浏览器](evidence/2026-09-07-artifact-revision/browser-tests.txt) | ChatPanel 初始化清空引用，详见 C→B 契约 |
| 真实模型读改存 | 报告/习题均保存 v2 | [模型证据](evidence/2026-09-07-artifact-revision/live-model.jsonl) | 其余类型真实模型/使用场景未验证 |
| Vite build | passed | [构建](evidence/2026-09-07-artifact-revision/build.txt) | 大 bundle 提示 |
| 全项目 tsc | 未通过；本次 C 新增/修改业务文件无报错 | [类型检查](evidence/2026-09-07-artifact-revision/typecheck.txt) | 仓库其他文件/旧测试及共享类型错误 |
| 与 B 集成 | 服务/组件已经被 B 引用，reply/SSE 四组合通过 | [C→B 契约](../plans/2026-09-07-artifact-revision-integration-cn.md) | 跨页引用竞态、历史/恢复 API UI、组合验收未完成 |

真实模型报告修改前包含 `C-REVISION-20260907` 独特标记；修改后标记仍在，案例增加两项。习题原选项/答案/解析保留，只改为明确询问 Python 列表首个下标。模型错误路径被校验拒绝后可修正重试，只有通过校验才保存。

实际生成目录覆盖报告、教案、博客、习题、闪卡、思维导图、游戏和 AI 课堂；报告大纲映射 report 存储类型。旧存储枚举还含 audio/video/ai_lecture_session，但不在当前生成工厂目录，未添加修改入口，不声称支持二进制修改。

### 回退与剩余限制

- 回退可移除 C UI 按钮并停用共享入口的 ArtifactRevisionService；既有资料、历史版本、旧导出均保留。不要删除 revision_history 或 MaterialVersion。
- JSON C 修改操作有线程＋进程间锁；旧手动写入器没有使用新增进程间锁，因此与旧多进程写入同时发生的情况未获完整保证。
- 当前完整读取原文，不做静默截断；超出模型上下文时失败并保留原文，按章节分段加载尚未实现。
- 恢复服务已实现，主 UI 的历史查看/恢复按钮仍需 B 路由接入。
- 课堂讲解变化会清除改动 Action 的旧 audioUrl/audioId 并标记需配音，实际重新配音/视频导出未验收。

验收结论：C 可交付模块及明确接入记录；整体未验收，不可宣布所有类型及前后端全链路全部完成。
