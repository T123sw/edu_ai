可以把 TVIR 理解成：**不是单纯“生成报告”，而是让报告里的文字、图片、图表都成为证据链的一部分**。

## 一、TVIR / TVIR-Agent 是什么

**TVIR** 全称是 **Text-Visual Interleaved Report Generation**，中文可以理解为“图文交织式报告生成”。它针对的问题是：现在很多 Deep Research Agent 能写长报告、能引用网页，但图片和图表经常只是装饰，没有真正参与论证，也缺少图文一致性和视觉证据质量评估。TVIR 项目明确指出，现有系统大多偏文本，只关注文本连贯性、深度和引用，对视觉证据的真实性、来源和上下文对齐评估不足。([nju-link.github.io][1])

TVIR 包含三部分：

1. **TVIR-Bench**：一个图文报告生成 benchmark，包含 100 个专家构造任务，覆盖 10 个领域，中英文各 50 个，并按 3 个复杂度等级组织。([nju-link.github.io][1])
2. **TVIR-Agent**：一个分层多智能体框架，用来生成图文交织式报告。([nju-link.github.io][1])
3. **Dual-Path Evaluation**：双路径评测，同时评估文本质量和视觉质量，比如引用支撑、写作质量、图像质量、图注质量、图文融合度、图表与来源一致性等。([nju-link.github.io][1])

它的核心思想是：**图片和图表不是写完报告后随便插进去，而是在规划阶段就被设计成报告的一部分。**

---

## 二、TVIR-Agent 的流程

TVIR-Agent 主要分 4 个阶段。

### 1. Research-Grounded Planning：带研究依据的规划

Planner 先解析用户任务，然后调用 Google Search、网页抓取等外部工具，收集资料，最后生成结构化大纲。这个大纲里不只是章节标题，还包括：

* 每节摘要；
* 每节需要什么视觉材料；
* 每节研究笔记；
* 引用来源、URL、关键发现。

这一步很关键，因为它不是直接写报告，而是先判断：**哪些地方需要文字论证，哪些地方需要图片，哪些地方需要图表。**([arXiv][2])

### 2. Visual Asset Instantiation：视觉资产实例化

这一阶段专门处理图片和图表。它用了两个专门组件：

* **Image Searcher**：处理人物、场景、架构图等视觉概念，通过 Google Image Search 找候选图，然后用启发式规则过滤低质量图片，再用 VQA 工具验证相关性。
* **Chart Generator**：处理数据分布、趋势、关系类内容，通过搜索和网页抓取找数据，验证数据真实性和跨来源一致性，再生成 Python 绘图代码，并在沙盒里执行生成图表。([arXiv][2])

这一点对你特别有参考价值：它把“找图片”和“生成图表”分开了。因为两者本质不同：图片更偏检索和筛选，图表更偏数据来源、计算和可视化生成。

### 3. Context-Aware Sequential Writing：上下文感知的逐节写作

Writer 不是一次性写完整篇报告，而是逐节生成。每一节都会参考当前大纲单元、已有全局上下文、研究笔记和对应视觉资产。写作时，它会决定图片或图表插入位置，并生成 Markdown 格式的图文混排内容。([arXiv][2])

这个设计也很适合你的系统，因为报告、教案、PPT 都不是纯聊天结果，而是结构化产物。逐节生成比一次性生成更容易控制质量。

### 4. Global Index Polishing：全局编号与引用整理

最后 Polisher 统一处理引用和图表编号，包括：

* 删除未引用的参考文献；
* 按 URL 和内容去重；
* 统一重编号；
* 更新正文里的引用标记；
* 重新分配图号和图题。([arXiv][2])

这个阶段对正式报告很重要。很多 Agent 生成报告时会出现“图1、图3、图3重复”“图片有但正文没引用”“参考文献没用上”等问题，TVIR 专门把这个做成最后的整理阶段。

---

## 三、TVIR 对你项目最大的启发

你不要只借鉴它的“搜图”部分，更应该借鉴它的整体思想：

> **在大纲阶段就规划视觉需求，而不是正文写完后再找图。**

比如你的报告生成可以这样设计：

```text
用户任务
  ↓
报告规划 Agent
生成大纲 + 每节写作目标 + 每节视觉需求
  ↓
资料检索 Agent
形成文本证据池
  ↓
视觉资产 Agent
找图片 / 生成图表 / 生成示意图
  ↓
写作 Agent
逐节生成图文混排报告
  ↓
审查 Agent
检查事实、引用、图文一致性、图号、图注
  ↓
导出器
Markdown / Word / PDF
```

这样你的项目就不是普通的“报告生成器”，而是可以讲成：

> **面向教学与学术场景的多智能体图文证据对齐内容生成系统。**

这个表述比“我做了一个能生成报告的 Agent”更有亮点。

---

## 四、你的系统应该做成“一个整体 Agent”，还是“主对话 Agent + 多个子 Agent”？

我的建议很明确：**用户感知上是一个整体 Agent，工程实现上用“主对话 Agent + 多个子 Agent / 工作流 Agent + 工具层”。**

不要做成一个巨大的单体 Agent。因为你的系统里有报告生成、教案生成、PPT 生成、知识库问答、图片搜索、文档解析、产物导出等多个能力，如果全塞进一个 Agent，会出现几个问题：

1. **职责混乱**：一个 Agent 既要聊天、又要规划、又要检索、又要生成 PPT、又要审查，prompt 会越来越臃肿。
2. **调试困难**：生成结果不好时，你很难判断是规划错了、检索错了、写作错了，还是导出错了。
3. **复用性差**：图片搜索、引用审查、知识库检索其实可以被报告、PPT、教案共同复用。
4. **质量不可控**：正式产物最好走固定流程和校验门，而不是完全靠 Agent 自由发挥。

更合适的结构是：

```text
主对话 Agent / Orchestrator
负责理解用户意图、选择任务类型、维护对话上下文
  ├── 报告生成 Agent
  ├── 教案生成 Agent
  ├── PPT 生成 Agent
  ├── 试题生成 Agent
  ├── 知识库问答 Agent
  ├── 视觉资产 Agent
  └── 审查 / 质量控制 Agent

共享工具层：
  ├── RAG 检索工具
  ├── Web 搜索工具
  ├── 图片搜索工具
  ├── VLM 图片审查工具
  ├── 图表生成工具
  ├── 文档导出工具
  └── 格式校验工具
```

也就是说，**产品入口统一，内部能力模块化**。

用户只看到一个聊天入口，比如：

> “帮我根据这份课程资料生成一份教学报告，并配上合适图片。”

主 Agent 判断这是“报告生成任务”，然后调用报告生成 Agent。报告生成 Agent 内部再调用知识库检索、图片搜索、图文审查和导出工具。

---

## 五、图片搜索应该做成工具，还是图片搜索 Agent？

这两个不是二选一，而是分层关系。

### 底层应该是“图片搜索工具”

图片搜索 API 本身应该封装成工具，比如：

```text
image_search_tool(query, style, license, count)
→ 返回候选图片列表
```

它只负责执行搜索，不负责复杂决策。类似：

* 根据 query 搜索图片；
* 返回图片 URL、来源页面、标题、尺寸、缩略图；
* 可选返回版权信息；
* 可选下载图片。

这个层面应该是工具，不应该是 Agent。

### 上层可以做“视觉资产 Agent”

但如果任务变复杂，比如：

> “给这份 RAG 报告每一节找一张合适的学术风格图片，要求能支撑正文，不要装饰图，最好有来源。”

这时就不只是一次搜索了，而是需要：

1. 理解章节内容；
2. 判断该节需不需要图；
3. 决定要找真实图片、架构图、流程图还是数据图；
4. 生成多组搜索词；
5. 多轮搜索；
6. 用 VLM 审查图片；
7. 生成图题和图注；
8. 保存来源和版权；
9. 返回可插入报告的视觉资产。

这就应该做成 **视觉资产 Agent / Image Asset Agent**。

所以我建议这样设计：

```text
Visual Asset Agent
  ├── image_search_tool
  ├── image_download_tool
  ├── image_quality_check_tool
  ├── vlm_relevance_review_tool
  ├── license_check_tool
  ├── chart_generation_tool
  └── caption_generation_tool
```

图片搜索工具只是它调用的一个工具。

---

## 六、结合你的系统，我建议这样落地

你现在系统里有报告、教案、PPT 等功能。最合理的演进路线是：

### 第一层：主对话 Agent

负责：

* 理解用户需求；
* 判断任务类型；
* 询问必要参数；
* 调用对应子 Agent；
* 把结果展示在右侧工作台。

### 第二层：任务型子 Agent

每个核心产物一个子 Agent：

| 子 Agent      | 负责内容                  |
| ------------ | --------------------- |
| 报告生成 Agent   | 生成调研报告、课程报告、技术报告      |
| 教案生成 Agent   | 生成教学目标、重难点、教学流程、课堂活动  |
| PPT 生成 Agent | 生成大纲、页面内容、视觉建议、PPT 文件 |
| 知识库问答 Agent  | 基于课程知识库和个人知识库回答问题     |
| 视觉资产 Agent   | 找图、生成图表、生成图注、审查图文一致性  |
| 审查 Agent     | 检查格式、引用、事实、图文一致性、输出质量 |

### 第三层：共享工具

工具不要绑定死在某个 Agent 里，而是共享：

```text
知识库检索工具
网页搜索工具
图片搜索工具
文档解析工具
图表生成工具
VLM 审查工具
格式校验工具
导出 Word/PDF/PPT 工具
```

这样报告 Agent 可以调用图片能力，PPT Agent 也可以调用图片能力，教案 Agent 如果需要课程插图也能调用图片能力。

---

## 七、你可以参考 TVIR，但不要完全照搬

TVIR 的重点是“深度研究报告”，而你的系统是“教育内容生成平台”。所以你可以吸收它的结构，但需要改成教育场景。

TVIR 里是：

```text
Planner → Image Searcher / Chart Generator → Writer → Polisher
```

你可以改成：

```text
教学任务理解
  ↓
产物规划 Agent
  ↓
课程知识检索 Agent
  ↓
内容生成 Agent
  ↓
视觉资产 Agent
  ↓
质量审查 Agent
  ↓
格式化与导出
```

对于报告生成，可以非常接近 TVIR；对于 PPT 生成，则要多加一个 **版式规划 / 页面容量控制 / 模板渲染**；对于教案生成，则要多加 **教学目标、学情分析、教学活动、评价设计** 这些教育结构。

---

## 八、最终建议

你的系统不要做成“一个万能大 Agent”，而应该做成：

> **一个主对话 Agent 作为统一入口，下面挂多个任务型子 Agent，每个子 Agent 内部是可控工作流；图片搜索封装成底层工具，同时在上层做一个视觉资产 Agent。**

图片搜索的定位最好是：

> **工具层负责搜图，视觉资产 Agent 负责理解需求、规划搜索、筛选图片、生成图注、检查图文一致性。**

这样既符合 Agent 项目的讲法，也方便工程实现。你后面面试或者和老师讨论时，可以强调：

1. 我不是简单调用大模型生成内容；
2. 我把教育内容生成拆成多个任务型 Agent；
3. 报告、PPT、教案共享知识库检索、视觉资产、质量审查能力；
4. 图片不是装饰，而是作为“视觉证据”参与内容生成；
5. 生成后有审查环节，检查事实、引用、格式和图文一致性。

这个方向是可以做成一个比较完整、能讲清楚架构亮点的 Agent 项目的。

[1]: https://nju-link.github.io/TVIR/ "TVIR: Text-Visual Interleaved Report Generation"
[2]: https://arxiv.org/html/2606.02320v1 "TVIR: Building Deep Research Agents Towards Text–Visual Interleaved Report Generation"
