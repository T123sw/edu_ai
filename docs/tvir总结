我仔细看了 TVIR 的论文、项目页和 GitHub。先给结论：**TVIR 的核心价值不是“它能搜图片”，而是它把图片、图表、引用、正文一起纳入报告生成流程，把视觉内容从“装饰素材”提升成“证据组件”。** 这点非常适合你做报告生成、PPT 生成、教案生成系统时参考。

## 1. TVIR 到底解决什么问题

TVIR 全称是 **Text–Visual Interleaved Report Generation**，即“图文交织式报告生成”。它针对的是 Deep Research Agent 的一个问题：现在很多系统能检索资料、写长报告、加引用，但视觉内容通常只是附加配图，缺少对图片来源、图文一致性、图表数据真实性的检查。TVIR 认为真实专业报告里，图片、图表、流程图不是装饰，而是用来支撑论点、展示趋势、帮助理解的“视觉证据”。([arXiv][1])

所以 TVIR 不是单纯的图片搜索项目，而是一个包含三部分的体系：

| 部分                       | 作用                     |
| ------------------------ | ---------------------- |
| **TVIR-Bench**           | 用来评测图文交织式深度研究报告生成能力    |
| **TVIR-Agent**           | 一个生成图文报告的多阶段多 Agent 框架 |
| **Dual-Path Evaluation** | 同时评估文本质量和视觉质量的双路径评测框架  |

项目页显示 TVIR-Bench 包含 **100 个专家构造任务、10 个领域、2 种语言、9 个系统评测**。([南京大学链接][2])

---

## 2. TVIR-Agent 的核心流程

TVIR-Agent 的整体流程可以概括成四步：

```text
用户研究任务
  ↓
1. Research-Grounded Planning
   生成结构化大纲 + 每节视觉需求 + 研究笔记
  ↓
2. Visual Asset Instantiation
   图片检索 + 图表生成 + 来源记录
  ↓
3. Context-Aware Sequential Writing
   逐节写作，插入图片/图表/引用
  ↓
4. Global Index Polishing
   统一引用、图号、去重、清理未引用来源
```

这四步里，最值得你关注的是前两步：**先规划视觉需求，再实例化视觉资产**。这和普通“写完报告后再找几张图”完全不一样。

---

## 3. 第一步：Research-Grounded Planning

Planner 会解析用户任务，然后调用 Google Search、网页抓取等工具检索资料，最后生成一个结构化大纲。这个大纲不是普通章节列表，而是每个章节单元都包含：

* section title：章节标题；
* brief summary：章节摘要；
* planned visual requirements：计划需要的视觉材料；
* section-level research notes：本节研究笔记；
* citation、source URL、key findings：引用、来源链接、关键发现。

论文明确说，research notes 用来给后续阶段提供事实依据，提高可信度和可追溯性。([arXiv][1])

这一步对你很关键。你后面做报告、教案、PPT 时，不应该只生成：

```text
第一章：RAG 概述
第二章：RAG 技术流程
第三章：应用案例
```

而应该生成更结构化的计划：

```json
{
  "section_title": "RAG 技术流程",
  "summary": "解释检索、重排、上下文组织、生成四个步骤",
  "visual_need": {
    "type": "流程图",
    "purpose": "帮助读者理解 RAG 管线",
    "search_queries": [
      "retrieval augmented generation workflow",
      "RAG architecture diagram"
    ]
  },
  "evidence_notes": [
    {
      "claim": "RAG 通常由检索器和生成器组成",
      "source_url": "...",
      "key_findings": "..."
    }
  ]
}
```

也就是说，**视觉需求要在大纲阶段就被规划出来**，而不是正文生成之后再临时配图。

---

## 4. 第二步：Visual Asset Instantiation

这是 TVIR-Agent 最有特色的部分。它把视觉资产分成两类处理：

| 视觉类型 | 处理模块                | 典型内容          |
| ---- | ------------------- | ------------- |
| 图片类  | **Image Searcher**  | 人物、场景、架构图、概念图 |
| 图表类  | **Chart Generator** | 趋势、分布、对比、数据关系 |

**Image Searcher** 负责通过 Google Image Search 检索候选图，用启发式规则过滤低质量图片，再用 VQA 工具验证相关性，最后选出最合适的图片。**Chart Generator** 负责检索数据、验证数据真实性和跨来源一致性，然后生成 Python 绘图代码，并在 sandbox 中执行生成图表。([arXiv][1])

这一点很值得你借鉴：**搜图片和生成图表不应该混在一个模块里。**

你的系统里也可以这样拆：

```text
Visual Asset Agent
  ├── Image Searcher：找真实图片、架构图、示意图
  ├── Chart Generator：根据数据生成统计图、趋势图
  ├── Diagram Generator：生成流程图、结构图、教学示意图
  └── Visual Reviewer：检查图片/图表是否符合正文
```

TVIR 还会把每个视觉资产的 caption、description、source provenance 写回大纲；对于代码生成图表，会保留原始数据来源 URL；对于检索图片，会保留来源网页 URL。([arXiv][1])

这说明它不是“下载一张图插进去”，而是把图片当成有来源、有解释、有归属的报告对象。

---

## 5. 第三步：Context-Aware Sequential Writing

TVIR-Agent 不是一次性生成整篇报告，而是**逐节写作**。Writer 会参考当前章节单元、已有全局上下文、研究笔记和视觉资产，然后生成 Markdown 格式的图文混排内容。为了减少重复，它会维护一个动态更新的 global context，包括前面已生成章节的标题、摘要和小节结构。([arXiv][1])

这一点也适合你的系统。比如 PPT 生成、教案生成、报告生成，都最好不要一次性让模型吐完整内容，而是：

```text
先规划结构
再逐章/逐页生成
每生成一部分就更新上下文
最后统一审查和格式化
```

对 PPT 来说尤其重要，因为每页容量有限；对报告来说，则有助于控制引用、图号和结构一致性。

---

## 6. 第四步：Global Index Polishing

最后 Polisher 会在报告级别处理引用和图表编号：

* 删除未被正文引用的参考文献；
* 按 URL 和规范化内容去重；
* 统一重编号；
* 更新正文引用标记；
* 重新分配图号和图题；
* 更新正文中的图表引用。

论文明确把这个作为最终阶段，专门解决报告中常见的“引用没用上、图号重复、图表编号混乱、正文引用对不上”等问题。([arXiv][1])

这个模块对你的系统非常实用。你现在做报告、测试文档、PPT，经常会遇到格式、编号、图表题注、交叉引用的问题。TVIR 的做法说明：**正式产物生成系统必须有最后的结构整理阶段，不能只靠生成模型一次性完成。**

---

## 7. TVIR-Bench 的评测思路

TVIR 的评测也很值得看。它不是只评“写得好不好”，而是分成 **Textual Assessment** 和 **Visual Assessment** 两条路径。

文本评测包括：

| 指标  | 含义                                     |
| --- | -------------------------------------- |
| CS  | Citation Support，事实是否被引用支持             |
| IA  | Instruction Alignment，是否满足任务要求         |
| WQ  | Writing Quality，写作质量                   |
| ADB | Analytical Depth & Breadth，分析深度与广度     |
| FLC | Factual & Logical Consistency，事实与逻辑一致性 |

视觉评测包括：

| 指标  | 含义                                   |
| --- | ------------------------------------ |
| MC  | Multimodal Composition，图文组合是否合理      |
| FQ  | Figure Quality，图片/图表质量               |
| FCQ | Figure Caption Quality，图注质量          |
| FCI | Figure-Context Integration，图片和上下文融合度 |
| CSC | Chart-Source Consistency，图表和数据来源是否一致 |

这些指标说明 TVIR 真正关心的是：**图片有没有用、图注准不准、图表数据有没有来源、图片是否融入正文，而不是报告里有没有几张图。**([arXiv][1])

这对你的项目特别有启发。你可以把它简化成自己的质量门：

```text
文本质量检查：
- 是否覆盖用户要求
- 是否有事实依据
- 是否存在逻辑矛盾

视觉质量检查：
- 是否有图
- 图是否清晰
- 图是否和本节内容相关
- 图注是否准确
- 正文是否引用了这张图
- 图片/图表是否有来源
```

---

## 8. 效果怎么样，怎么看待它的分数

从论文结果看，TVIR-Agent 的几个版本在 TVIR-Bench 上整体表现最好。比如 GitHub README 里的主结果显示，TVIR-Agent Claude-4.5-Sonnet 版本 Overall 为 74.44，Qwen3-Max 版本为 73.53，GLM-4.7 版本为 72.62；对比系统里 Manus-1.6 为 69.73，Claude-4.5-Sonnet w/Search 为 68.72。([GitHub][3])

但这个结果要谨慎看。因为 TVIR-Bench 是作者提出的 benchmark，TVIR-Agent 也是作者自己的系统，所以它更能说明：**在这个评测框架下，显式规划视觉需求、分离图片/图表模块、统一整理引用图号确实有效**。但不能直接等价为“TVIR 在所有真实场景都比商业系统强”。

消融实验更有价值。论文显示，去掉 Chart Generator 后，VA 从 78.62 降到 60.91，Overall 从 73.92 降到 63.84；去掉 Image Searcher 后，Overall 也从 73.92 降到 72.53。这说明在 TVIR 任务里，图表生成比单纯图片搜索影响更大，但图片搜索也确实有贡献。([arXiv][1])

对你来说，这个结论很重要：**如果只是做“找图片”，亮点有限；如果能做“图片 + 数据图表 + 图文一致性审查 + 来源追溯”，项目价值会高很多。**

---

## 9. GitHub 工程成熟度

GitHub 仓库目前比较新，页面显示只有少量 star、fork 和 commit，目录包含 `agent/`、`benchmark/`、`docs/`、`libs/miroflow-tools/` 等部分。([GitHub][3])

运行上，它要求 Python 3.12+、uv，并且需要配置 Serper API、E2B API、OpenAI API、VQA 模型等。README 里还说明，Agent 可从 benchmark 任务运行，也可以在 `agent/main.py` 里写自定义任务描述后运行。([GitHub][3])

所以我对它的判断是：

> **TVIR 更像一个论文原型和研究基线，不是成熟可直接商用的开源产品。**

你可以读它的设计，参考它的 pipeline，但不建议你直接把它当作你系统的底座。

---

## 10. 对你系统最有用的核心总结

我觉得 TVIR 最值得你吸收的不是代码，而是这几个设计原则。

### 第一，视觉内容前置规划

不要等正文生成完再配图，而是在大纲阶段就决定：

```text
这一节需不需要图？
需要什么类型的图？
这张图要支撑什么观点？
图片/图表从哪里来？
```

### 第二，图片和图表分开处理

图片检索和图表生成是两类任务：

```text
图片检索：偏搜索、筛选、版权、相关性
图表生成：偏数据来源、数值一致性、代码绘图
```

你的系统里最好不要只做一个 `image_search_tool`，而是做成：

```text
Visual Asset Agent
  ├── Image Search Tool
  ├── Chart Generation Tool
  ├── Diagram Generation Tool
  ├── VLM Review Tool
  └── Source/Copyright Check Tool
```

### 第三，写作要逐节/逐页生成

报告按章节生成，PPT 按页面生成，教案按模块生成。每生成一部分，就把标题、摘要、视觉资产、引用记录更新到全局上下文中。

### 第四，必须有最后的 Polisher

最后统一处理：

```text
引用编号
图号
图题
图注
未引用参考文献
重复图片
断链图片
正文未提及的图片
```

这个模块在工程里非常重要，而且能明显提升正式产物质量。

### 第五，评测也要多模态

你不能只评“内容写得好不好”，还要评：

```text
图片是否相关
图片是否清晰
图注是否准确
图片是否真的支撑正文
图表数据是否有来源
正文是否引用了图片
```

---

## 11. 如果迁移到你的教育系统，可以这样改造

TVIR 原始目标是 deep research report。你的系统有报告、教案、PPT，所以可以改成：

```text
主对话 Agent
  ↓
产物规划 Agent
  - 判断是报告 / 教案 / PPT
  - 生成结构化大纲
  - 标记每章/每页视觉需求
  ↓
资料检索 Agent
  - 检索课程知识库
  - 检索用户上传资料
  - 必要时检索外部网页
  ↓
视觉资产 Agent
  - 搜图
  - 生成流程图/示意图
  - 生成数据图表
  - VLM 审查图文相关性
  ↓
内容生成 Agent
  - 报告逐节写
  - PPT 逐页写
  - 教案按教学环节写
  ↓
质量审查 Agent
  - 内容覆盖
  - 事实依据
  - 格式规范
  - 图文一致性
  - 引用/图号/题注
  ↓
导出器
  - Word / PDF / PPT / HTML
```

你可以把项目亮点表述为：

> 本系统借鉴 TVIR 的图文交织式生成思想，在报告、PPT、教案等教育产物生成中引入“视觉需求规划—视觉资产实例化—图文一致性审查—全局编号整理”的流程，使图片和图表不再作为后处理装饰，而是作为可追溯、可审查、可复用的教学视觉资产参与内容生成。

这个方向比“我做了一个能生成报告的 Agent”更有辨识度。TVIR 给你的最大启发就是：**Agent 的核心不是多调用几个工具，而是把产物生成拆成可规划、可验证、可追溯的结构化流程。**

[1]: https://arxiv.org/html/2606.02320v1 "TVIR: Building Deep Research Agents Towards Text–Visual Interleaved Report Generation"
[2]: https://nju-link.github.io/TVIR/ "TVIR: Text-Visual Interleaved Report Generation"
[3]: https://github.com/NJU-LINK/TVIR "GitHub - NJU-LINK/TVIR: A benchmark and multi-agent framework for text-visual interleaved deep research report generation · GitHub"
