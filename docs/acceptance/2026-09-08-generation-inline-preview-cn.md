# 生成工厂文件原位预览

教师工作台右侧“最近生成”不再显示“让 AI 修改”。点击文件后，生成工厂区域切换为文件预览并加宽；顶部“返回生成工厂”恢复文件列表与原宽度，页面地址和对话区域保持不变。

复用资源预览与课堂播放组件。普通文件通过现有鉴权接口读取，支持加载提示、失败重试；关闭预览或切换课程时忽略过期请求。桌面布局加宽右栏，窄屏抽屉在后续收窄调整中设为最多 600px。

验证：浏览器覆盖 1366、1920、1024 三种窗口的原位打开、加宽、返回及读取失败重试；前端构建与既有布局、文件展示单元检查。
