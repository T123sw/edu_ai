# 修改前讨论与失败诊断

截图 20 的任务 `revision-f4cda83261cb492b21a5bb95dfa3` 于 2026-09-08 08:28:18 UTC 失败。后端日志记录 REVISION_FAILED；只读复现发现模型调用抛出 APIStatusError。三个已配置模型分别验证均返回 HTTP 402 / Insufficient Balance。对应报告仍为 v1，未写入新内容。诊断执行阻止保存，没有重试或重跑用户修改任务。

原流程在读取目标后立即提交修改任务，技能允许意见明确时直接出补丁。本次改为：

1. 读取原文，在对话中逐轮澄清受众、目标、范围和需要的细节。
2. 给出具体修改位置、改动建议和理由，询问是否采用。
3. 用户调整要求时更新方案并再次确认；只读插问保留方案，不授权执行。
4. 仅在后续消息确认已展示方案后提交修改任务，工作器携带已确认方案并重新校验原版本。初始请求即使详细也不能跳过方案展示。

讨论阶段没有修改任务与版本写入。应用技能区分 PLANNING_PROMPT 和 SYSTEM_PROMPT，代码拒绝讨论阶段的 edits、没有已有方案的 confirm，以及没有 approved_plan 的后台任务。公开错误不含服务商内部信息；日志记录操作号、异常类别和 HTTP 状态，便于诊断。

验证：152 项隔离后端检查通过，覆盖资料存储与版本、共享回复入口、知识范围、讨论状态、多轮方案变更、插问、确认后队列执行和错误诊断。技能格式检查及 git diff --check 通过。本次无前端修改。真实模型修改测试因服务商余额不足未能完成，需恢复服务余额或可用配置后运行更新后的 artifact_revision_skill_live_smoke.py；不能据此声称真实修改已恢复。
