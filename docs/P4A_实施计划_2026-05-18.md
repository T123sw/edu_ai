# P4-A ReAct Agent化 — 完整实施计划

**日期**：2026-05-18  
**基于规格**：`docs/P4A_ReAct_Agent化_设计规格_2026-05-18.md`  
**总工期**：7天（Phase 1~2 = 验证 agent 行为；Phase 3~5 = 接真实任务）

---

## Phase 0：代码库探查结论（已完成）

### 关键接口确认

| 接口 | 文件 | 签名 |
|------|------|------|
| `submit_workflow_task` | `app/chat/tasks/background_runner.py` | `(*, workflow, request, snapshot, decision, workflow_type="", on_complete=None) -> str` |
| 所有 workflow `run()` | 各 workflow `runtime.py` | `(self, *, request, snapshot, decision)` |
| `stream_chat` | `app/chat/model_gateway.py:112` | `(messages, temperature=0.2, max_tokens=1200) -> Iterable[str]` |
| `RouteDecision.path` | `app/chat/domain/route_decision.py` | `Literal["fast", "workflow"]` ← 需加 "agent" |
| `CapabilityPolicy` | `app/chat/domain/capability_policy.py` | `allow_rag`, `allow_web`, `allow_tools`, `max_tool_steps` |

### 重要发现
- `agent_tools.py` **不存在**，完全新建
- Report / LessonPlan workflow 通过 `gathered_context.get("slot_hints")` 读取预填槽位
- **PPT 和 Quiz 不使用 gathered_context**，需要独立的注入机制
- `submit_workflow_task()` 必须传入 `decision` 参数，agent 调用时需构造合成的 `RouteDecision`
- 现有 SSE 事件类型：`metadata` / `delta` / `result` / `status` / `task_submitted`

---

## Phase 1：Foundation（1.5天）

**目标**：建立三个基础模块：`stream_chat_with_tools()`、`agent_tools.py`（含 stub 工具）、`route_decision.py` 扩展。

**完成后可验证**：单独运行每个模块的单元测试，无需启动服务。

---

### Task 1.1：扩展 RouteDecision（10分钟）

**文件**：`app/chat/domain/route_decision.py`

**改动**：第9行，`path` 字面量加 `"agent"`

```python
# 改前
path: Literal["fast", "workflow"]
# 改后
path: Literal["fast", "workflow", "agent"]
```

**新增类方法**：
```python
@classmethod
def agent(cls, *, reason: str) -> "RouteDecision":
    return cls(path="agent", action="agent.plan", reason=reason)
```

**验证**：
```python
from app.chat.domain.route_decision import RouteDecision
d = RouteDecision.agent(reason="test")
assert d.path == "agent"
```

---

### Task 1.2：新增 stream_chat_with_tools()（半天）

**文件**：`app/chat/model_gateway.py`

**在 `stream_chat()` 方法之后新增**：

```python
def stream_chat_with_tools(
    self,
    messages: List[Dict[str, Any]],
    tools: List[Dict[str, Any]],
    tool_choice: str = "auto",
    temperature: float = 0.1,
    max_tokens: int = 1024,
) -> Iterable[Dict[str, Any]]:
    """流式调用，支持 function calling。
    yield 事件格式：
      {"type": "text_delta", "content": str}   ← 立即 yield，保证 TTFT
      {"type": "tool_calls", "calls": list}     ← finish_reason=tool_calls 后 yield
      {"type": "done"}
      {"type": "unsupported"}                   ← 模型不支持工具时
      {"type": "error", "message": str}
    """
    errors: List[str] = []
    for candidate in self.candidates:
        url = f"{candidate['api_base']}/chat/completions"
        payload: Dict[str, Any] = {
            "model": candidate["model_name"],
            "messages": messages,
            "tools": tools,
            "tool_choice": tool_choice,
            "temperature": temperature,
            "max_tokens": max_tokens,
            "stream": True,
            **self._thinking_params(candidate["model_name"]),
        }
        t0 = time.perf_counter()
        t_first: float | None = None
        try:
            resp = requests.post(
                url,
                headers=self._build_headers(candidate.get("api_key")),
                json=payload,
                stream=True,
                timeout=120,
            )
        except requests.RequestException as exc:
            errors.append(f"{candidate['model_name']}: {exc}")
            continue
        if resp.status_code != 200:
            errors.append(f"{candidate['model_name']}: HTTP {resp.status_code}")
            continue

        # 积累 tool_calls（分批到达，需合并）
        accumulated_tool_calls: Dict[int, Dict[str, Any]] = {}
        has_tool_calls = False

        for raw_line in resp.iter_lines(decode_unicode=False):
            if not raw_line:
                continue
            line = (raw_line.decode("utf-8", errors="replace") if isinstance(raw_line, bytes) else raw_line).strip()
            if line.startswith("data:"):
                line = line[len("data:"):].strip()
            if line == "[DONE]":
                break
            try:
                chunk = json.loads(line)
            except json.JSONDecodeError:
                continue
            choices = chunk.get("choices") or []
            if not choices:
                continue
            choice = choices[0]
            delta = choice.get("delta") or {}
            finish_reason = choice.get("finish_reason")

            # ── 文字内容：立即 yield ──────────────────────────────
            content = delta.get("content")
            if content:
                if t_first is None:
                    t_first = time.perf_counter()
                    print(f"[AGENT] ⚡ 首token {(t_first - t0)*1000:.0f}ms", flush=True)
                yield {"type": "text_delta", "content": str(content)}

            # ── 工具调用：积累 ────────────────────────────────────
            for tc in (delta.get("tool_calls") or []):
                idx = tc.get("index", 0)
                if idx not in accumulated_tool_calls:
                    accumulated_tool_calls[idx] = {
                        "id": tc.get("id", ""),
                        "type": "function",
                        "function": {"name": "", "arguments": ""},
                    }
                fn = tc.get("function") or {}
                if fn.get("name"):
                    accumulated_tool_calls[idx]["function"]["name"] += fn["name"]
                if fn.get("arguments"):
                    accumulated_tool_calls[idx]["function"]["arguments"] += fn["arguments"]
                if tc.get("id"):
                    accumulated_tool_calls[idx]["id"] = tc["id"]
                has_tool_calls = True

            if finish_reason == "tool_calls":
                calls = []
                for tc in sorted(accumulated_tool_calls.values(), key=lambda x: 0):
                    try:
                        args = json.loads(tc["function"]["arguments"] or "{}")
                    except json.JSONDecodeError:
                        args = {}
                    calls.append({
                        "id": tc["id"],
                        "name": tc["function"]["name"],
                        "args": args,
                    })
                print(f"[AGENT] tool_calls: {[c['name'] for c in calls]} {(time.perf_counter()-t0)*1000:.0f}ms", flush=True)
                yield {"type": "tool_calls", "calls": calls}
                yield {"type": "done"}
                return

            if finish_reason == "stop":
                yield {"type": "done"}
                return

        # 流结束但未命中 finish_reason（某些模型行为）
        if accumulated_tool_calls:
            calls = []
            for tc in sorted(accumulated_tool_calls.values(), key=lambda x: 0):
                try:
                    args = json.loads(tc["function"]["arguments"] or "{}")
                except json.JSONDecodeError:
                    args = {}
                calls.append({"id": tc["id"], "name": tc["function"]["name"], "args": args})
            yield {"type": "tool_calls", "calls": calls}
        yield {"type": "done"}
        return

    yield {"type": "error", "message": f"所有模型失败: {' | '.join(errors)}"}
```

**验证**：
```python
# test: 纯文字响应
events = list(gw.stream_chat_with_tools(messages, tools=[]))
assert any(e["type"] == "text_delta" for e in events)
assert events[-1]["type"] == "done"

# test: 工具调用响应（mock HTTP）
# finish_reason=tool_calls 时必须 yield tool_calls 事件
```

---

### Task 1.3：创建 agent_tools.py（半天）

**新文件**：`app/chat/runtime/agent_tools.py`

```python
"""P4-A Agent 工具层：7个工具的 Schema + 执行函数。

Phase 1：generate_* 全部为 stub（返回假 task_id），draft_outline 为真实 LLM 调用。
Phase 4：generate_* 替换为真实 submit_workflow_task。
"""
from __future__ import annotations

import json
import time
from typing import Any

from core.config import Config


# ── 工具 JSON Schema ──────────────────────────────────────────────────────────

_SCHEMA_RAG_SEARCH = {
    "type": "function",
    "function": {
        "name": "rag_search",
        "description": (
            "当用户明确提到'我的资料/知识库/上传的文件'，或问题需要基于用户私有内容回答时调用。"
            "通用知识、概念解释、一般问答不调用。"
        ),
        "parameters": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "检索关键词"},
                "top_k": {"type": "integer", "description": "返回结果数", "default": 5},
            },
            "required": ["query"],
        },
    },
}

_SCHEMA_WEB_SEARCH = {
    "type": "function",
    "function": {
        "name": "web_search",
        "description": (
            "当用户需要实时信息、最新动态、当前数据时调用（'今天/最新/当前'为典型信号）。"
            "历史概念、通用知识不调用。"
        ),
        "parameters": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "搜索关键词"},
            },
            "required": ["query"],
        },
    },
}

_SCHEMA_DRAFT_OUTLINE = {
    "type": "function",
    "function": {
        "name": "draft_outline",
        "description": (
            "当你已收集到足够信息（主题明确），需要生成结构化大纲供用户确认时调用。"
            "调用后将返回的大纲展示给用户，询问是否需要调整。"
            "resource_type 填写 'report'/'ppt'/'lesson_plan'。"
        ),
        "parameters": {
            "type": "object",
            "properties": {
                "resource_type": {
                    "type": "string",
                    "enum": ["report", "ppt", "lesson_plan"],
                    "description": "资源类型",
                },
                "subject": {"type": "string", "description": "主题/课题"},
                "focus": {"type": "string", "description": "重点方向（可选）", "default": ""},
                "constraints": {"type": "string", "description": "用户的补充约束，如'加一节量子纠错'", "default": ""},
                "slide_count": {"type": "integer", "description": "PPT 页数（resource_type=ppt 时有效）", "default": 10},
                "grade": {"type": "string", "description": "年级（resource_type=lesson_plan 时有效）", "default": ""},
                "duration_minutes": {"type": "integer", "description": "课时分钟数", "default": 45},
            },
            "required": ["resource_type", "subject"],
        },
    },
}

_SCHEMA_GENERATE_REPORT = {
    "type": "function",
    "function": {
        "name": "generate_report",
        "description": (
            "仅在用户已确认大纲内容后调用（必须传入 confirmed_outline）。"
            "这会触发后台报告生成任务，不可中断，确认前不要调用。"
        ),
        "parameters": {
            "type": "object",
            "properties": {
                "subject": {"type": "string", "description": "报告主题"},
                "confirmed_outline": {"type": "string", "description": "用户已确认的大纲（Markdown 格式）"},
                "focus": {"type": "string", "description": "报告重点", "default": ""},
                "length_hint": {"type": "string", "description": "字数要求（如'5000字'）", "default": ""},
            },
            "required": ["subject", "confirmed_outline"],
        },
    },
}

_SCHEMA_GENERATE_PPT = {
    "type": "function",
    "function": {
        "name": "generate_ppt",
        "description": (
            "仅在用户已确认PPT大纲后调用（必须传入 confirmed_outline）。"
            "会触发后台PPT生成任务，不可中断。"
        ),
        "parameters": {
            "type": "object",
            "properties": {
                "topic": {"type": "string", "description": "PPT 主题"},
                "confirmed_outline": {"type": "string", "description": "用户已确认的PPT大纲（Markdown格式）"},
                "slide_count": {"type": "integer", "description": "幻灯片数量", "default": 10},
            },
            "required": ["topic", "confirmed_outline"],
        },
    },
}

_SCHEMA_GENERATE_LESSON_PLAN = {
    "type": "function",
    "function": {
        "name": "generate_lesson_plan",
        "description": (
            "仅在用户已确认教案大纲后调用（必须传入 confirmed_outline）。"
        ),
        "parameters": {
            "type": "object",
            "properties": {
                "subject": {"type": "string", "description": "课题名称"},
                "confirmed_outline": {"type": "string", "description": "用户已确认的教案大纲"},
                "grade": {"type": "string", "description": "年级", "default": ""},
                "duration_minutes": {"type": "integer", "description": "课时分钟数", "default": 45},
            },
            "required": ["subject", "confirmed_outline"],
        },
    },
}

_SCHEMA_GENERATE_QUIZ = {
    "type": "function",
    "function": {
        "name": "generate_quiz",
        "description": (
            "当用户明确要求生成练习题/习题/测试题时直接调用（无需大纲步骤）。"
            "从对话中提取主题、题量、难度后调用。"
        ),
        "parameters": {
            "type": "object",
            "properties": {
                "subject": {"type": "string", "description": "题目主题"},
                "question_count": {"type": "integer", "description": "题目数量", "default": 10},
                "difficulty": {
                    "type": "string",
                    "enum": ["easy", "medium", "hard"],
                    "default": "medium",
                },
                "question_types": {
                    "type": "array",
                    "items": {"type": "string", "enum": ["choice", "blank", "short", "judge"]},
                    "description": "题型列表，空列表表示混合",
                    "default": [],
                },
            },
            "required": ["subject"],
        },
    },
}

_ALL_GENERATE_SCHEMAS = [
    _SCHEMA_DRAFT_OUTLINE,
    _SCHEMA_GENERATE_REPORT,
    _SCHEMA_GENERATE_PPT,
    _SCHEMA_GENERATE_LESSON_PLAN,
    _SCHEMA_GENERATE_QUIZ,
]


def build_tool_schemas(capability) -> list[dict]:
    """根据 capability 动态构建工具列表（capability 门控）。"""
    schemas = []
    if getattr(capability, "allow_rag", False):
        schemas.append(_SCHEMA_RAG_SEARCH)
    if getattr(capability, "allow_web", False):
        schemas.append(_SCHEMA_WEB_SEARCH)
    schemas.extend(_ALL_GENERATE_SCHEMAS)
    return schemas


# ── 工具执行 ──────────────────────────────────────────────────────────────────

_TOOL_TO_ACTION_HINT = {
    "generate_report":      "generate.report",
    "generate_ppt":         "generate.ppt",
    "generate_lesson_plan": "generate.lesson_plan",
    "generate_quiz":        "generate.quiz",
}
_TOOL_TO_WORKFLOW = {
    "generate_report":      "report",
    "generate_ppt":         "ppt",
    "generate_lesson_plan": "lesson_plan",
    "generate_quiz":        "quiz",
}


class ToolExecutionContext:
    def __init__(self, *, capability, max_steps: int, rag_retriever=None,
                 web_retriever=None, workflow_registry=None, background_runner=None,
                 agent_gateway=None, request=None, snapshot=None):
        self.capability = capability
        self.max_steps = max_steps
        self.rag_retriever = rag_retriever
        self.web_retriever = web_retriever
        self.workflow_registry = workflow_registry or {}
        self.background_runner = background_runner
        self.agent_gateway = agent_gateway
        self.request = request
        self.snapshot = snapshot
        self.step_count = 0
        self.trace: dict[str, Any] = {"agent_steps": []}
        self._call_cache: dict[str, Any] = {}

    def already_called(self, name: str, args: dict) -> bool:
        key = f"{name}:{json.dumps(args, sort_keys=True)}"
        return key in self._call_cache

    def cache_result(self, name: str, args: dict, result: Any) -> None:
        key = f"{name}:{json.dumps(args, sort_keys=True)}"
        self._call_cache[key] = result

    def get_cached_result(self, name: str, args: dict) -> Any:
        key = f"{name}:{json.dumps(args, sort_keys=True)}"
        return self._call_cache[key]


def execute_tool(name: str, args: dict, ctx: ToolExecutionContext) -> dict:
    """工具执行入口，含 pre/post 横切检查。"""
    # ── Pre-checks ──────────────────────────────────────────────────────────
    if ctx.step_count >= ctx.max_steps:
        return {"ok": False, "tool": name, "error": "budget_exceeded", "summary": "已达最大工具调用次数"}
    if not _capability_allows(name, ctx.capability):
        return {"ok": False, "tool": name, "error": "permission_denied", "summary": "capability 不允许此工具"}
    if ctx.already_called(name, args):
        return ctx.get_cached_result(name, args)

    # ── 执行 ─────────────────────────────────────────────────────────────────
    t0 = time.perf_counter()
    try:
        result = _dispatch(name, args, ctx)
        result["ok"] = result.get("ok", True)
    except Exception as exc:
        result = {"ok": False, "tool": name, "error": str(exc), "summary": f"工具执行失败: {exc}"}
    elapsed_ms = round((time.perf_counter() - t0) * 1000)

    # ── Observability ─────────────────────────────────────────────────────────
    ctx.trace["agent_steps"].append({
        "step": ctx.step_count,
        "tool": name,
        "args": {k: str(v)[:100] for k, v in args.items()},
        "result_summary": result.get("summary", ""),
        "ok": result.get("ok", False),
        "duration_ms": elapsed_ms,
    })
    ctx.step_count += 1
    ctx.cache_result(name, args, result)
    return result


def _capability_allows(name: str, capability) -> bool:
    if name == "rag_search" and not getattr(capability, "allow_rag", False):
        return False
    if name == "web_search" and not getattr(capability, "allow_web", False):
        return False
    return True


def _dispatch(name: str, args: dict, ctx: ToolExecutionContext) -> dict:
    if name == "rag_search":
        return _exec_rag_search(args, ctx)
    if name == "web_search":
        return _exec_web_search(args, ctx)
    if name == "draft_outline":
        return _exec_draft_outline(args, ctx)
    if name in _TOOL_TO_WORKFLOW:
        return _exec_generate_stub(name, args, ctx)  # Phase 1: stub
    return {"ok": False, "tool": name, "error": "unknown_tool", "summary": f"未知工具: {name}"}


def _exec_rag_search(args: dict, ctx: ToolExecutionContext) -> dict:
    if ctx.rag_retriever is None:
        return {"ok": False, "tool": "rag_search", "error": "rag_not_available", "summary": "RAG 检索器未配置"}
    query = str(args.get("query", ""))
    top_k = int(args.get("top_k", 5))
    try:
        result = ctx.rag_retriever(
            query=query, top_k=top_k,
            selected_doc_ids=list(getattr(ctx.capability, "selected_doc_ids", []) or []),
            owner=getattr(ctx.request, "owner", None),
        )
        payload = dict((result or {}).get("payload") or {}) if isinstance(result, dict) else {}
        answer = str(payload.get("answer") or "").strip()
        sources = list(payload.get("sources") or [])
        return {
            "ok": True, "tool": "rag_search",
            "summary": f"检索到 {len(sources)} 条结果",
            "payload": {"answer": answer, "sources": sources},
        }
    except Exception as exc:
        return {"ok": False, "tool": "rag_search", "error": str(exc), "summary": f"RAG 检索失败: {exc}"}


def _exec_web_search(args: dict, ctx: ToolExecutionContext) -> dict:
    if ctx.web_retriever is None:
        return {"ok": False, "tool": "web_search", "error": "web_not_available", "summary": "Web 检索器未配置"}
    query = str(args.get("query", ""))
    try:
        result = ctx.web_retriever(query=query, owner=getattr(ctx.request, "owner", None))
        payload = dict((result or {}).get("payload") or {}) if isinstance(result, dict) else {}
        summary = str(payload.get("summary") or payload.get("answer") or "").strip()
        sources = list(payload.get("sources") or [])
        return {
            "ok": True, "tool": "web_search",
            "summary": f"联网检索完成，获取 {len(sources)} 个来源",
            "payload": {"summary": summary, "sources": sources},
        }
    except Exception as exc:
        return {"ok": False, "tool": "web_search", "error": str(exc), "summary": f"Web 检索失败: {exc}"}


_DRAFT_OUTLINE_PROMPTS = {
    "report": (
        "请为以下报告生成一份结构清晰的大纲（Markdown 格式，用 # 表示章节）。\n"
        "报告主题：{subject}\n重点方向：{focus}\n补充约束：{constraints}\n\n"
        "只输出大纲，不要其他说明。"
    ),
    "ppt": (
        "请为以下PPT生成幻灯片大纲（Markdown 格式，每页用 ## 标题，下方 - 列要点）。\n"
        "PPT主题：{subject}\n幻灯片数量：{slide_count}张\n补充约束：{constraints}\n\n"
        "只输出大纲，不要其他说明。"
    ),
    "lesson_plan": (
        "请为以下教案生成教学大纲（Markdown 格式）。\n"
        "课题：{subject}\n年级：{grade}\n课时：{duration_minutes}分钟\n补充约束：{constraints}\n\n"
        "只输出大纲，不要其他说明。"
    ),
}


def _exec_draft_outline(args: dict, ctx: ToolExecutionContext) -> dict:
    resource_type = str(args.get("resource_type", "report"))
    subject = str(args.get("subject", ""))
    prompt_template = _DRAFT_OUTLINE_PROMPTS.get(resource_type, _DRAFT_OUTLINE_PROMPTS["report"])
    prompt = prompt_template.format(
        subject=subject,
        focus=str(args.get("focus", "")),
        constraints=str(args.get("constraints", "")),
        slide_count=int(args.get("slide_count", 10)),
        grade=str(args.get("grade", "")),
        duration_minutes=int(args.get("duration_minutes", 45)),
    )
    try:
        outline_text = ctx.agent_gateway.chat(
            [{"role": "user", "content": prompt}],
            temperature=0.3,
            max_tokens=800,
        )
        return {
            "ok": True, "tool": "draft_outline",
            "summary": f"已生成 {resource_type} 大纲（{len(outline_text)} 字符）",
            "payload": {"outline_markdown": outline_text, "resource_type": resource_type, "subject": subject},
        }
    except Exception as exc:
        return {"ok": False, "tool": "draft_outline", "error": str(exc), "summary": f"大纲生成失败: {exc}"}


# ── Phase 1 Stubs（Phase 2 替换为真实调用） ───────────────────────────────────

def _exec_generate_stub(name: str, args: dict, ctx: ToolExecutionContext) -> dict:
    """Phase 1 stub：立即返回假 task_id，用于验证 agent 对话流程。"""
    import uuid
    workflow_type = _TOOL_TO_WORKFLOW[name]
    task_id = f"stub-{workflow_type}-{uuid.uuid4().hex[:8]}"
    print(f"[STUB] {name}({args}) → {task_id}", flush=True)
    return {
        "ok": True, "tool": name,
        "summary": f"已提交 {workflow_type} 生成任务（stub）",
        "payload": {"task_id": task_id, "workflow_type": workflow_type},
    }
```

**验证**：
```bash
# 在 api/src 目录下
python -c "
from app.chat.runtime.agent_tools import build_tool_schemas, execute_tool, ToolExecutionContext
from app.chat.domain.capability_policy import CapabilityPolicy

cap = CapabilityPolicy(allow_rag=True, allow_web=False)
schemas = build_tool_schemas(cap)
names = [s['function']['name'] for s in schemas]
assert 'rag_search' in names
assert 'web_search' not in names
assert 'draft_outline' in names
assert 'generate_quiz' in names
print('OK:', names)
"
```

---

## Phase 2：ReActAgent + Orchestrator（1.5天）

**目标**：ReActAgent 完整实现，接入 Orchestrator，服务可启动并处理 agent 路径请求。

---

### Task 2.1：创建 react_agent.py（1天）

**新文件**：`app/chat/runtime/react_agent.py`

```python
"""P4-A ReAct Agent — 全程对话编排。

对外接口：ReActAgent.run_stream(*, request, snapshot) -> Iterator[dict]

SSE 事件序列：
  {type: "status", payload: {stage: "thinking", label: "..."}}
  {type: "delta",  payload: {content: "..."}}        ← 追问/大纲/普通回答
  {type: "tool_call", payload: {tool: str, args: dict}}
  {type: "tool_result", payload: {tool: str, summary: str, payload: dict}}
  {type: "task_submitted", payload: {task_id: str, workflow_type: str, message: str}}
  {type: "result", payload: {...}}                    ← 最终结果（兼容现有格式）
"""
from __future__ import annotations

import time
import uuid
from typing import Any, Callable, Iterator, Optional

from core.config import Config
from app.chat.runtime.agent_tools import (
    ToolExecutionContext, build_tool_schemas, execute_tool,
)

# ── Agent 系统提示 ────────────────────────────────────────────────────────────

AGENT_SYSTEM_PROMPT = """你是一个教学资源助手，帮助用户生成报告、PPT、教案和练习题。

【信息收集原则】
- 如果缺少必要信息（主题/课题），先通过自然对话追问，不要调用任何工具
- 一次最多问 2 个问题，不要列清单式追问

【大纲确认流程】（报告/PPT/教案需要，练习题不需要）
1. 信息充足后，调用 draft_outline 工具生成大纲
2. 将大纲内容展示给用户，询问是否需要调整
3. 用户确认后（说"可以"/"好"/"开始"等），调用对应 generate_* 工具，传入 confirmed_outline

【工具调用原则】
- generate_* 工具只在用户明确确认大纲后调用，必须传入 confirmed_outline 参数
- generate_quiz 无大纲步骤，用户明确要求时直接调用
- 调用 generate_* 后，告知用户任务已在后台处理
- 普通问答直接回答，不调用任何工具

【语气要求】
- 自然、简洁，避免"请输入'确认并继续'"等命令式提示
- 追问时不超过 2 个问题"""


class ReActAgent:
    def __init__(
        self,
        *,
        agent_gateway,
        fast_runtime,
        rag_retriever=None,
        web_retriever=None,
        workflow_registry=None,
        background_runner=None,
        max_steps: int | None = None,
        timeout_seconds: float | None = None,
    ):
        self.agent_gateway = agent_gateway
        self.fast_runtime = fast_runtime
        self.rag_retriever = rag_retriever
        self.web_retriever = web_retriever
        self.workflow_registry = workflow_registry or {}
        self.background_runner = background_runner
        self.max_steps = max_steps if max_steps is not None else Config.REACT_MAX_STEPS
        self.timeout_seconds = timeout_seconds if timeout_seconds is not None else Config.REACT_TIMEOUT_SECONDS

    def run_stream(self, *, request, snapshot) -> Iterator[dict]:
        t_start = time.perf_counter()
        trace_id = str(uuid.uuid4())
        capability = getattr(snapshot, "capability", None)

        yield {"type": "status", "payload": {"stage": "thinking", "label": "正在分析请求..."}}

        tool_schemas = build_tool_schemas(capability)
        ctx = ToolExecutionContext(
            capability=capability,
            max_steps=self.max_steps,
            rag_retriever=self.rag_retriever,
            web_retriever=self.web_retriever,
            workflow_registry=self.workflow_registry,
            background_runner=self.background_runner,
            agent_gateway=self.agent_gateway,
            request=request,
            snapshot=snapshot,
        )
        ctx.trace["trace_id"] = trace_id
        ctx.trace["path"] = "agent"

        messages = self._build_messages(request, snapshot)

        try:
            yield from self._react_loop(messages, tool_schemas, ctx, request, snapshot, t_start)
        except _AgentTimeout:
            print(f"[AGENT] 超时 fallback", flush=True)
            yield from self._fallback(request, snapshot, reason="react_timeout")
        except Exception as exc:
            print(f"[AGENT] 异常 fallback: {exc}", flush=True)
            yield from self._fallback(request, snapshot, reason=f"react_error: {exc}")

    def _react_loop(
        self, messages: list, tool_schemas: list, ctx: ToolExecutionContext,
        request, snapshot, t_start: float
    ) -> Iterator[dict]:
        stream_chat_with_tools = getattr(self.agent_gateway, "stream_chat_with_tools", None)
        if not callable(stream_chat_with_tools):
            yield from self._fallback(request, snapshot, reason="gateway_no_tools_support")
            return

        answer_chunks: list[str] = []
        task_submitted_events: list[dict] = []

        while True:
            if (time.perf_counter() - t_start) > self.timeout_seconds:
                raise _AgentTimeout()

            # ── 调用 LLM ──────────────────────────────────────────────────────
            events = list(stream_chat_with_tools(
                messages,
                tool_schemas,
                tool_choice="auto",
                temperature=0.1,
                max_tokens=1024,
            ))

            # ── 处理 unsupported ──────────────────────────────────────────────
            if any(e["type"] == "unsupported" for e in events):
                yield from self._fallback(request, snapshot, reason="unsupported_function_calling")
                return

            # ── 处理 error ────────────────────────────────────────────────────
            if any(e["type"] == "error" for e in events):
                err_msg = next((e["message"] for e in events if e["type"] == "error"), "未知错误")
                yield from self._fallback(request, snapshot, reason=f"llm_error: {err_msg}")
                return

            # ── 流式转发文字 + 收集 tool_calls ────────────────────────────────
            tool_calls_event = None
            for e in events:
                if e["type"] == "text_delta":
                    answer_chunks.append(e["content"])
                    yield {"type": "delta", "payload": {"content": e["content"]}}
                elif e["type"] == "tool_calls":
                    tool_calls_event = e

            # ── 纯文字响应：结束循环 ──────────────────────────────────────────
            if tool_calls_event is None:
                break

            # ── 工具调用 ──────────────────────────────────────────────────────
            tool_results_for_messages = []
            for call in tool_calls_event["calls"]:
                tool_name = call["name"]
                tool_args = call["args"]
                call_id = call.get("id", f"call_{tool_name}")

                yield {"type": "tool_call", "payload": {"tool": tool_name, "args": tool_args}}

                result = execute_tool(tool_name, tool_args, ctx)

                yield {"type": "tool_result", "payload": {
                    "tool": tool_name,
                    "summary": result.get("summary", ""),
                    "ok": result.get("ok", False),
                }}

                # task_submitted 事件（generate_* 工具成功时）
                if result.get("ok") and tool_name in _TOOL_TO_WORKFLOW:
                    payload = result.get("payload", {})
                    task_id = payload.get("task_id", "")
                    workflow_type = payload.get("workflow_type", "")
                    if task_id:
                        label = _WORKFLOW_LABELS.get(workflow_type, "内容")
                        task_submitted_events.append({
                            "type": "task_submitted",
                            "payload": {
                                "task_id": task_id,
                                "workflow_type": workflow_type,
                                "message": f"正在后台生成{label}，可通过任务ID查询进度",
                            },
                        })

                # 注入 tool_result 到消息历史
                tool_result_content = _format_tool_result_for_context(tool_name, result)
                tool_results_for_messages.append({
                    "tool_call_id": call_id,
                    "role": "tool",
                    "name": tool_name,
                    "content": tool_result_content,
                })

            # 将 assistant 的 tool_calls + 工具结果注入消息历史
            messages = messages + [
                {
                    "role": "assistant",
                    "tool_calls": [
                        {
                            "id": c.get("id", f"call_{c['name']}"),
                            "type": "function",
                            "function": {
                                "name": c["name"],
                                "arguments": __import__("json").dumps(c["args"], ensure_ascii=False),
                            },
                        }
                        for c in tool_calls_event["calls"]
                    ],
                },
                *tool_results_for_messages,
            ]

        # ── 发出 task_submitted 事件 ──────────────────────────────────────────
        for evt in task_submitted_events:
            yield evt

        # ── 最终 result 事件 ──────────────────────────────────────────────────
        total_ms = round((time.perf_counter() - ctx.trace.get("_t_start", 0)) * 1000)
        ctx.trace["total_ms"] = total_ms
        answer = "".join(answer_chunks)
        yield {
            "type": "result",
            "payload": {
                "message": {"role": "assistant", "content": answer},
                "conversation": {"conversation_id": getattr(request, "conversation_id", "") or ""},
                "action": {"name": "agent.reply"},
                "artifacts": [],
                "workflow": None,
                "sources": [],
                "trace": ctx.trace,
            },
        }

    def _build_messages(self, request, snapshot) -> list[dict]:
        recent = list(getattr(snapshot, "recent_messages", []) or [])
        history = []
        for msg in recent:
            role = str((msg.get("role") if isinstance(msg, dict) else getattr(msg, "role", "user")) or "user")
            content = str((msg.get("content") if isinstance(msg, dict) else getattr(msg, "content", "")) or "")
            if content:
                history.append({"role": role, "content": content})
        return [
            {"role": "system", "content": AGENT_SYSTEM_PROMPT},
            *history,
            {"role": "user", "content": str(getattr(request, "question", "") or "")},
        ]

    def _fallback(self, request, snapshot, *, reason: str) -> Iterator[dict]:
        print(f"[AGENT] fallback reason={reason}", flush=True)
        yield {"type": "status", "payload": {"stage": "fallback", "label": "切换到直接回答模式"}}
        from app.chat.domain.route_decision import RouteDecision
        decision = RouteDecision.fast(action="chat.reply", reason=reason)
        yield from self.fast_runtime.run_stream(request=request, snapshot=snapshot, decision=decision)


class _AgentTimeout(Exception):
    pass


_TOOL_TO_WORKFLOW = {
    "generate_report": "report",
    "generate_ppt": "ppt",
    "generate_lesson_plan": "lesson_plan",
    "generate_quiz": "quiz",
}

_WORKFLOW_LABELS = {
    "report": "报告",
    "ppt": "PPT课件",
    "lesson_plan": "教案",
    "quiz": "练习题",
}


def _format_tool_result_for_context(tool_name: str, result: dict) -> str:
    if not result.get("ok"):
        return f"工具 {tool_name} 执行失败: {result.get('error', '未知错误')}"
    payload = result.get("payload", {})
    if tool_name == "rag_search":
        return f"知识库检索结果：\n{payload.get('answer', '无内容')}"
    if tool_name == "web_search":
        return f"联网检索结果：\n{payload.get('summary', '无内容')}"
    if tool_name == "draft_outline":
        return f"已生成大纲：\n{payload.get('outline_markdown', '')}"
    if tool_name in _TOOL_TO_WORKFLOW:
        return f"已提交后台任务，task_id={payload.get('task_id', '')}"
    return result.get("summary", "")
```

**验证**：
```python
# mock test
from unittest.mock import MagicMock
agent = ReActAgent(
    agent_gateway=MagicMock(),
    fast_runtime=MagicMock(),
)
# mock stream_chat_with_tools 返回纯文字响应
agent.agent_gateway.stream_chat_with_tools.return_value = [
    {"type": "text_delta", "content": "量子纠缠是"},
    {"type": "text_delta", "content": "一种..."},
    {"type": "done"},
]
events = list(agent.run_stream(request=MagicMock(question="量子纠缠是什么"), snapshot=MagicMock()))
assert any(e["type"] == "delta" for e in events)
assert any(e["type"] == "result" for e in events)
```

---

### Task 2.2：修改 main_orchestrator.py（0.5天）

**文件**：`app/chat/orchestrator/main_orchestrator.py`

**新增 `react_agent` 参数，修改 `dispatch_stream()`**：

```python
from core.config import Config

_HINT_TO_WORKFLOW = {
    "generate.report": "report",
    "generate.ppt": "ppt",
    "generate.lesson_plan": "lesson_plan",
    "generate.quiz": "quiz",
    "research.lookup": "research",
}

class MainOrchestrator:
    def __init__(self, *, fast_runtime, workflow_registry, context_builder,
                 react_agent=None):          # ← 新增，可选
        self.fast_runtime = fast_runtime
        self.workflow_registry = workflow_registry
        self.context_builder = context_builder
        self.react_agent = react_agent       # ← 新增

    def dispatch_stream(self, request, *, on_workflow_complete=None):
        snapshot = self.context_builder.build(request)
        action_hint = str(getattr(request, "action_hint", "") or "")

        # Level 1: action_hint 确定路由 → Workflow 完整路径（不变）
        if action_hint and action_hint in _HINT_TO_WORKFLOW:
            workflow_name = _HINT_TO_WORKFLOW[action_hint]
            if workflow_name not in self.workflow_registry:
                yield from self.fast_runtime.run_stream(
                    request=request, snapshot=snapshot,
                    decision=RouteDecision.fast(action="chat.reply", reason="workflow_unregistered"),
                )
                return
            yield from self._dispatch_workflow(request, snapshot, workflow_name, on_workflow_complete)
            return

        # Level 2: Agent 路径
        if self.react_agent is not None and Config.USE_REACT_AGENT:
            yield from self.react_agent.run_stream(request=request, snapshot=snapshot)
            return

        # Fallback: 旧 route_rules（USE_REACT_AGENT=false）
        decision = decide_route(
            request=request, snapshot=snapshot,
            workflow_state=getattr(snapshot, "workflow_state", None),
        )
        if decision.path == "fast":
            yield from self.fast_runtime.run_stream(request=request, snapshot=snapshot, decision=decision)
            return
        if decision.workflow_name not in self.workflow_registry:
            yield from self.fast_runtime.run_stream(
                request=request, snapshot=snapshot,
                decision=RouteDecision.fast(action="chat.reply", reason="workflow_unregistered_fallback"),
            )
            return
        yield from self._dispatch_workflow(request, snapshot, decision.workflow_name, on_workflow_complete)

    def _dispatch_workflow(self, request, snapshot, workflow_name, on_workflow_complete):
        from app.chat.tasks.background_runner import submit_workflow_task
        from app.chat.domain.route_decision import RouteDecision
        label = _WORKFLOW_LABELS.get(workflow_name, "内容")
        decision = RouteDecision(
            path="workflow", action=f"generate.{workflow_name}",
            workflow_name=workflow_name, reason="level1_action_hint",
        )
        task_id = submit_workflow_task(
            workflow=self.workflow_registry[workflow_name],
            request=request, snapshot=snapshot, decision=decision,
            workflow_type=workflow_name, on_complete=on_workflow_complete,
        )
        yield {"type": "status", "payload": {
            "stage": "task_queued",
            "label": f"{label}生成任务已提交，正在后台处理...",
            "workflow": {"type": workflow_name, "status": "running"},
        }}
        yield {"type": "task_submitted", "payload": {
            "task_id": task_id, "workflow_type": workflow_name,
            "message": f"正在后台生成{label}，可通过任务ID查询进度",
        }}

    # dispatch() 方法不变
```

**验证**：
```bash
# 启动服务，发一条普通问题
curl -N http://localhost:8000/api/chat/v2/stream \
  -d '{"question":"量子纠缠是什么","conversation_id":"test123"}' \
  -H "Content-Type: application/json"
# 期望：看到 delta 事件流，最后有 result 事件
# 期望：日志中有 "[AGENT]" 前缀输出
```

---

### Task 2.3：DI 工厂注入 react_agent（0.5天）

**文件 1**：`app/chat/runtime/model_registry.py`

新增 `build_agent_gateway()`，避免把 `Config.get_agent_model()` 的虚拟 id 传给 `Config.get_llm_model()`：

```python
def build_agent_gateway() -> ChatModelGateway:
    model = Config.get_agent_model()
    candidate = _to_gateway_candidate(model)
    return ChatModelGateway(
        api_base=candidate["api_base"],
        api_key=candidate["api_key"],
        model_name=candidate["model_name"],
        fallbacks=[],
    )
```

**文件 2**：`app/chat/application/reply_service_v2.py`

该文件是主路径，`build_default_reply_service_v2()` 内的 `build_orchestrator(request)` 构建完整的 report / ppt / lesson_plan / quiz registry。修改方式：

1. import：

```python
from core.config import Config
from app.chat.runtime.react_agent import ReActAgent
from app.chat.runtime.model_registry import build_agent_gateway, build_default_gateway
from app.chat.tasks import background_runner
```

2. 在 `return MainOrchestrator(...)` 前，把 workflow_registry 提取成局部变量：

```python
workflow_registry = {
    "report": ReportWorkflowRuntime(
        engine_resolver=lambda *, request, snapshot, decision: build_default_report_engine(
            allow_rag=bool(getattr(request.capability, "allow_rag", False)),
            allow_web=bool(getattr(request.capability, "allow_web", False)),
        ),
        generation_context_builder=GenerationContextBuilder(),
        report_assembler=ReportAssembler(),
        report_context_organizer=ReportContextOrganizer(llm=get_fallback_llm()),
        generation_readiness_judge=GenerationReadinessJudge(),
    ),
    "ppt": PptWorkflowRuntime(
        generation_context_builder=GenerationContextBuilder(),
        ppt_context_organizer=PptContextOrganizer(llm=get_fallback_llm()),
        readiness_judge=PptReadinessJudge(),
        outline_builder=PptOutlineBuilder(llm=get_fallback_llm()),
        content_markdown_generator=PptContentMarkdownGenerator(llm=get_fallback_llm()),
        content_validator=PptContentValidator(),
        html2ppt_client_factory=lambda: Html2PptClient(
            base_url=os.getenv("HTML2PPT_BASE_URL", "http://127.0.0.1:46080")
        ),
    ),
    "lesson_plan": LessonPlanWorkflowRuntime(
        engine_resolver=lambda *, request, snapshot, decision: build_default_lesson_plan_engine(
            llm=get_fallback_llm()
        ),
        generation_context_builder=GenerationContextBuilder(),
        lesson_plan_context_organizer=LessonPlanContextOrganizer(),
        lesson_plan_readiness_judge=LessonPlanReadinessJudge(),
    ),
    "quiz": QuizWorkflowRuntime(
        generation_context_builder=GenerationContextBuilder(),
        quiz_context_organizer=QuizContextOrganizer(llm=get_fallback_llm()),
        quiz_readiness_judge=QuizReadinessJudge(),
    ),
}
```

3. 构建 `react_agent`，并传给 orchestrator：

```python
react_agent = None
if Config.USE_REACT_AGENT:
    react_agent = ReActAgent(
        agent_gateway=build_agent_gateway(),
        fast_runtime=fast_runtime,
        rag_retriever=rag_search_tool,
        web_retriever=web_search_tool,
        workflow_registry=workflow_registry,
        background_runner=background_runner,
        max_steps=Config.REACT_MAX_STEPS,
        timeout_seconds=Config.REACT_TIMEOUT_SECONDS,
    )

return MainOrchestrator(
    fast_runtime=fast_runtime,
    workflow_registry=workflow_registry,
    context_builder=context_builder,
    react_agent=react_agent,
)
```

**文件 3**：`app/chat/application/route_chat_service.py`

这是旧的 `RouteChatService` 构建路径。保留兼容：只在该路径构建了 `fast_runtime` 和 `workflow_registry` 后注入 agent。若该 service 的 workflow registry 未启用 ppt/quiz，也没关系，agent 的 generate_ppt/generate_quiz 工具执行时会返回 `workflow_unregistered`，不会破坏旧路径。

```python
from core.config import Config
from app.chat.runtime.react_agent import ReActAgent
from app.chat.runtime.model_registry import build_agent_gateway
from app.chat.tasks import background_runner

react_agent = None
if Config.USE_REACT_AGENT:
    react_agent = ReActAgent(
        agent_gateway=build_agent_gateway(),
        fast_runtime=fast_runtime,
        rag_retriever=None,
        web_retriever=None,
        workflow_registry=workflow_registry,
        background_runner=background_runner,
        max_steps=Config.REACT_MAX_STEPS,
        timeout_seconds=Config.REACT_TIMEOUT_SECONDS,
    )
```

然后把 `react_agent=react_agent` 加入 `MainOrchestrator(...)`。旧路径没有显式注入 RAG/Web 检索器，因此这里传 `None`，并依赖 `CapabilityPolicy` 让检索工具不可见；主路径 `reply_service_v2.py` 才启用真实 `rag_search_tool` / `web_search_tool`。

**验证**：

```bash
cd Edu_AI/api/src
python -m pytest tests -q
python - <<'PY'
from app.chat.runtime.model_registry import build_agent_gateway
gw = build_agent_gateway()
assert gw.candidates[0]["model_name"]
print(gw.candidates[0]["model_name"])
PY
```

预期：pytest 不因 DI import 失败；脚本输出 `deepseek-v4-flash` 或 `.env` 中 `REACT_AGENT_MODEL` 的值。

---

## Phase 3：Workflow Fast-Path（1.5天）

**目标**：4个 workflow 支持 agent 传入 `confirmed_outline` 时跳过收集和大纲阶段。

**注意**：PPT 和 Quiz 不使用 `gathered_context`，需要独立注入机制。所有 workflow 通过 `getattr(request, "_agent_params", {})` 读取 agent 注入的参数。

---

### Task 3.1：报告 Workflow Fast-Path

**文件**：`app/chat/workflows/report/runtime.py`

**Step 3.1.1**：在 `run()` 中解析 engine 后、进入原有 `GenerationContext` 构建前插入 fast-path：

```python
def run(self, *, request, snapshot, decision):
    # ── Agent Fast-Path ───────────────────────────────────────────────────────
    agent_params = dict(getattr(request, "_agent_params", {}) or {})
    confirmed_outline = str(agent_params.get("confirmed_outline", "")).strip()
    subject = str(agent_params.get("subject", "")).strip()
    if confirmed_outline and subject:
        return self._run_fast_generation(
            request=request, snapshot=snapshot, decision=decision,
            subject=subject, confirmed_outline=confirmed_outline,
            focus=str(agent_params.get("focus", "")),
            length_hint=str(agent_params.get("length_hint", "")),
        )
    # ── 原有完整路径（不改动）────────────────────────────────────────────────
```

**Step 3.1.2**：新增 `_run_fast_generation()`，用现有 report engine 的 state 协议直接进入生成：

```python
def _run_fast_generation(self, *, request, snapshot, decision, subject: str,
                         confirmed_outline: str, focus: str = "", length_hint: str = "") -> dict:
    engine = self.engine
    if self._engine_resolver is not None:
        engine = self._engine_resolver(request=request, snapshot=snapshot, decision=decision)
    if engine is None:
        raise KeyError("report")

    capability = getattr(request, "capability", None)
    generation_context = GenerationContext(
        conversation_id=request.conversation_id or "",
        resource_type="report",
    )
    gathered_context = {}
    if snapshot is not None:
        generation_context = self.generation_context_builder.build_for_resource(
            request=request,
            snapshot=snapshot,
            resource_type="report",
        )
        gathered_context = self.report_assembler.from_generation_context(generation_context)
        gathered_context["active_task"] = getattr(snapshot, "active_task", None)

    preparation = {
        "report_subject": subject,
        "report_focus": focus,
        "length_hint": length_hint,
        "preparation_source": "react_agent",
        "preparation_model": "confirmed_outline",
    }
    state = {
        "user_input": getattr(request, "question", ""),
        "human_feedback": "确认",
        "report_state": getattr(snapshot, "workflow_state", None) if snapshot is not None else None,
        "conversation_id": request.conversation_id or "",
        "owner": getattr(request, "owner", None),
        "allow_rag": bool(getattr(capability, "allow_rag", False)),
        "allow_web": bool(getattr(capability, "allow_web", False)),
        "selected_doc_ids": list(getattr(capability, "selected_doc_ids", []) or []),
        "gathered_context": gathered_context,
        "report_preparation_result": preparation,
        "readiness_decision": {"action": "agent_fast_path", "missing_critical_fields": []},
        "report_slots": {
            "core_topic": subject,
            "focus_area": focus,
            "length_hint": length_hint,
        },
        "report_outline": confirmed_outline,
        "soft_confirmed": True,
        "generation_ready": True,
    }
    _trace_context_injection(state)
    raw = engine.invoke(state)
    return self._build_response_from_engine_raw(
        request=request,
        raw=raw,
        trace_extra={
            "path": "workflow",
            "workflow_name": "report",
            "agent_fast_path": True,
            "readiness_decision": state["readiness_decision"],
        },
    )
```

**Step 3.1.3**：如果当前文件没有 `_build_response_from_engine_raw()`，先抽取现有两处 `engine.invoke()` 后的 response 组装逻辑为私有方法；抽取时保持字段不变：`message`、`conversation`、`action`、`artifacts`、`workflow`、`sources`、`trace`。

**验证**：

```python
request._agent_params = {
    "subject": "量子计算",
    "focus": "综述",
    "confirmed_outline": "## 一、基础\n- 量子比特",
}
result = runtime.run(request=request, snapshot=snapshot, decision=decision)
assert result["action"]["name"] == "generate.report"
assert result["trace"]["agent_fast_path"] is True
```

---

### Task 3.2：PPT Workflow Fast-Path

**文件**：`app/chat/workflows/ppt/runtime.py`

**Step 3.2.1**：新增 import：

```python
from app.chat.domain.ppt_outline import PptOutline, PptOutlineChapter, PptOutlineSlide
from app.chat.domain.ppt_preparation import PptPreparationResult
```

**Step 3.2.2**：在 `run()` 开头、现有 `workflow_state` 恢复逻辑之前插入：

```python
def run(self, *, request, snapshot, decision):
    # ── Agent Fast-Path ───────────────────────────────────────────────────────
    agent_params = dict(getattr(request, "_agent_params", {}) or {})
    confirmed_outline_md = str(agent_params.get("confirmed_outline", "")).strip()
    topic = str(agent_params.get("topic", "")).strip()
    if confirmed_outline_md and topic:
        ppt_outline = _parse_markdown_to_ppt_outline(confirmed_outline_md, topic)
        if ppt_outline is not None:
            return self._run_from_outline(
                request=request, snapshot=snapshot, decision=decision,
                ppt_outline=ppt_outline,
                slide_count=int(agent_params.get("slide_count", 10)),
                agent_params=agent_params,
            )
    # ── 原有完整路径 ──────────────────────────────────────────────────────────
```

**Step 3.2.3**：新增 `_parse_markdown_to_ppt_outline()`。PPT 真实类型是 `PptOutline(deck_title, chapters, slides)`，不是 `{topic, slides}` dict：

```python
def _parse_markdown_to_ppt_outline(markdown: str, topic: str) -> "PptOutline | None":
    """将 Markdown 大纲解析为 PptOutline，失败返回 None（降级到完整路径）。"""
    try:
        lines = markdown.strip().splitlines()
        slides: list[PptOutlineSlide] = []
        current_title = ""
        current_bullets = []

        def flush_slide() -> None:
            nonlocal current_title, current_bullets
            title = current_title.strip()
            if not title:
                return
            slide_index = len(slides) + 1
            slides.append(
                PptOutlineSlide(
                    slide_index=slide_index,
                    role="content" if slide_index > 1 else "cover",
                    title=title,
                    goal=f"讲清楚：{title}",
                    key_points=list(current_bullets),
                )
            )
            current_title = ""
            current_bullets = []

        for line in lines:
            stripped = line.strip()
            if not stripped:
                continue
            if stripped.startswith("#") or stripped[:2].rstrip(".、").isdigit():
                flush_slide()
                current_title = stripped.lstrip("#").strip(" .、")
            elif stripped.startswith("- ") or stripped.startswith("* "):
                current_bullets.append(stripped.lstrip("-* ").strip())
            elif current_title:
                current_bullets.append(stripped)
            else:
                current_title = stripped
        if current_title:
            flush_slide()
        if not slides:
            return None
        return PptOutline(
            deck_title=topic,
            confirmation_status="confirmed",
            chapters=[
                PptOutlineChapter(
                    chapter_index=1,
                    chapter_title=topic,
                    chapter_goal=f"围绕 {topic} 完成结构化讲解",
                    slides=slides,
                )
            ],
            slides=slides,
        )
    except Exception:
        return None
```

**Step 3.2.4**：新增 `_run_from_outline()`，复用现有 `_submit_outline()`：

```python
def _run_from_outline(self, *, request, snapshot, decision, ppt_outline: PptOutline,
                      slide_count: int, agent_params: dict) -> dict:
    preparation = PptPreparationResult(
        topic=str(agent_params.get("topic") or ppt_outline.deck_title),
        objective=str(agent_params.get("objective") or "课堂讲授"),
        audience=str(agent_params.get("grade") or ""),
        key_points=[],
        page_count=slide_count,
        theme=str(agent_params.get("theme_id") or ppt_outline.theme_id or ""),
        preparation_source="react_agent",
        preparation_model="confirmed_outline",
    )
    return self._submit_outline(
        request=request,
        outline=ppt_outline,
        preparation=preparation,
        followup_rounds=0,
    )
```

**验证**：

```python
outline = _parse_markdown_to_ppt_outline("## 光的干涉\n- 双缝干涉", "波动光学")
assert outline is not None
assert outline.deck_title == "波动光学"
assert outline.slides[0].title == "光的干涉"
assert outline.slides[0].key_points == ["双缝干涉"]
```

---

### Task 3.3：教案 Workflow Fast-Path

**文件**：`app/chat/workflows/lesson_plan/runtime.py`

**Step 3.3.1**：在 `run()` 解析 engine 后、调用 `lesson_plan_context_organizer.organize()` 前插入：

```python
agent_params = dict(getattr(request, "_agent_params", {}) or {})
confirmed_outline = str(agent_params.get("confirmed_outline", "")).strip()
subject = str(agent_params.get("subject", "")).strip()
if confirmed_outline and subject:
    return self._run_fast_generation(
        request=request,
        snapshot=snapshot,
        decision=decision,
        subject=subject,
        confirmed_outline=confirmed_outline,
        grade=str(agent_params.get("grade", "")),
        duration_minutes=int(agent_params.get("duration_minutes", 45)),
    )
```

**Step 3.3.2**：新增 `_run_fast_generation()`：

```python
def _run_fast_generation(self, *, request, snapshot, decision, subject: str,
                         confirmed_outline: str, grade: str, duration_minutes: int) -> dict:
    engine = self.engine
    if self._engine_resolver is not None:
        engine = self._engine_resolver(request=request, snapshot=snapshot, decision=decision)
    if engine is None:
        raise KeyError("lesson_plan")

    capability = getattr(request, "capability", None)
    generation_context = GenerationContext(
        conversation_id=_clean(getattr(snapshot, "conversation_id", "") or getattr(request, "conversation_id", "") or ""),
        resource_type="lesson_plan",
    )
    gathered_context: dict[str, Any] = {}
    if snapshot is not None:
        generation_context = self.generation_context_builder.build_for_resource(
            request=request,
            snapshot=snapshot,
            resource_type="lesson_plan",
        )
        gathered_context = self.lesson_plan_assembler.from_generation_context(generation_context)

    preparation = {
        "subject": subject,
        "grade": grade,
        "duration_minutes": duration_minutes,
        "preparation_source": "react_agent",
    }
    state = {
        "user_input": getattr(request, "question", ""),
        "human_feedback": "确认",
        "lesson_plan_state": getattr(snapshot, "workflow_state", None) if snapshot is not None else None,
        "conversation_id": _clean(getattr(request, "conversation_id", "") or ""),
        "owner": getattr(request, "owner", None),
        "allow_rag": bool(getattr(capability, "allow_rag", False)),
        "allow_web": bool(getattr(capability, "allow_web", False)),
        "selected_doc_ids": list(getattr(capability, "selected_doc_ids", []) or []),
        "gathered_context": gathered_context,
        "lesson_plan_preparation_result": preparation,
        "readiness_decision": {"action": "agent_fast_path", "missing_critical_fields": []},
        "lesson_plan_slots": {
            "subject": subject,
            "grade": grade,
            "duration_minutes": duration_minutes,
        },
        "lesson_plan_outline": confirmed_outline,
        "soft_confirmed": True,
        "generation_ready": True,
    }
    raw = engine.invoke(state)
    return self._build_response_from_engine_raw(
        request=request,
        raw=raw,
        state=state,
        trace_extra={"agent_fast_path": True},
    )
```

**Step 3.3.3**：如果没有 `_build_response_from_engine_raw()`，抽取 `run()` 中 `raw = engine.invoke(state)` 后的 response 组装逻辑，保持原有 artifact 补全逻辑不变。

**验证**：

```python
request._agent_params = {
    "subject": "波动光学",
    "grade": "高中物理",
    "duration_minutes": 45,
    "confirmed_outline": "## 教学目标\n- 理解干涉",
}
result = runtime.run(request=request, snapshot=snapshot, decision=decision)
assert result["action"]["name"] == "generate.lesson_plan"
assert result["trace"]["agent_fast_path"] is True
```

---

### Task 3.4：Quiz Workflow Fast-Path

**文件**：`app/chat/workflows/quiz/runtime.py`

Quiz 无大纲步骤，fast-path 只需跳过收集阶段：

```python
agent_params = dict(getattr(request, "_agent_params", {}) or {})
subject = str(agent_params.get("subject", "")).strip()
if subject and getattr(request, "_agent_fast_path", False):
    return self._run_fast_generation(
        request=request, snapshot=snapshot, decision=decision,
        subject=subject,
        question_count=int(agent_params.get("question_count", 10)),
        difficulty=str(agent_params.get("difficulty", "medium")),
        question_types=list(agent_params.get("question_types", [])),
    )
```

新增 `_run_fast_generation()`，直接调用现有 `quiz_generator.generate()`：

```python
def _run_fast_generation(self, *, request, snapshot, decision, subject: str,
                         question_count: int, difficulty: str,
                         question_types: list[str]) -> dict:
    capability = getattr(request, "capability", None)
    generation_context = GenerationContext(
        conversation_id=_clean(getattr(request, "conversation_id", "")),
        resource_type="quiz",
    )
    if snapshot is not None:
        generation_context = self.generation_context_builder.build_for_resource(
            request=request,
            snapshot=snapshot,
            resource_type="quiz",
        )
    preparation_payload = {
        "subject": subject,
        "question_count": question_count,
        "difficulty": difficulty,
        "question_types": question_types,
        "source": "react_agent",
    }
    artifact = self.quiz_generator.generate(
        preparation=preparation_payload,
        context_summary=generation_context.summary_text,
        conversation_id=_clean(getattr(request, "conversation_id", "")),
        owner=getattr(request, "owner", None),
        allow_rag=bool(getattr(capability, "allow_rag", False)),
        selected_doc_ids=list(getattr(capability, "selected_doc_ids", []) or []),
    )
    return self._finalize_response(
        {
            "message": {"role": "assistant", "content": "已生成，请在右侧查看。"},
            "conversation": {"conversation_id": _clean(getattr(request, "conversation_id", ""))},
            "action": {"name": "generate.quiz"},
            "artifacts": [artifact],
            "workflow": {
                "type": "quiz",
                "status": "completed",
                "stage": "generating",
                "required_slots": [],
                "filled_slots": {
                    "subject": subject,
                    "question_count": question_count,
                    "difficulty": difficulty,
                    "question_types": question_types,
                },
            },
            "sources": [],
            "trace": {
                "path": "workflow",
                "workflow_name": "quiz",
                "agent_fast_path": True,
                "quiz_preparation_result": preparation_payload,
                "readiness_decision": {"action": "agent_fast_path", "missing_critical_fields": []},
            },
        }
    )
```

**验证**：

```python
request._agent_fast_path = True
request._agent_params = {"subject": "Python 基础", "question_count": 10, "difficulty": "medium"}
result = runtime.run(request=request, snapshot=snapshot, decision=decision)
assert result["workflow"]["stage"] == "generating"
assert result["trace"]["agent_fast_path"] is True
```

---

## Phase 4：connect stubs to real background tasks（0.5天）

**目标**：将 `agent_tools.py` 中的 `_exec_generate_stub()` 替换为真实 `submit_workflow_task()` 调用。

**文件**：`app/chat/runtime/agent_tools.py`

**Step 4.1**：新增 `_clone_request()` 工具函数：

```python
def _clone_request(request) -> Any:
    """浅拷贝 request，保留 conversation_id / owner / capability。"""
    import copy
    try:
        cloned = copy.copy(request)
    except Exception:
        # fallback: 构造最小 request dict
        cloned = type("Request", (), {})()
        for attr in ("conversation_id", "owner", "capability", "question"):
            setattr(cloned, attr, getattr(request, attr, None))
    return cloned
```

**Step 4.2**：替换 `_exec_generate_stub()`：

```python
def _exec_generate_real(name: str, args: dict, ctx: ToolExecutionContext) -> dict:
    """Phase 2 真实实现：注入 _agent_params 并提交 workflow 后台任务。"""
    from app.chat.domain.route_decision import RouteDecision
    workflow_name = _TOOL_TO_WORKFLOW[name]
    action_hint = _TOOL_TO_ACTION_HINT[name]

    aug_request = _clone_request(ctx.request)
    # 注入 agent 参数（workflow fast-path 通过 _agent_params 读取）
    aug_request._agent_params = dict(args)
    aug_request._agent_fast_path = True
    aug_request.action_hint = action_hint

    decision = RouteDecision(
        path="workflow", action=action_hint,
        workflow_name=workflow_name, reason="agent_tool_call",
    )

    try:
        task_id = ctx.background_runner.submit_workflow_task(
            workflow=ctx.workflow_registry[workflow_name],
            request=aug_request,
            snapshot=ctx.snapshot,
            decision=decision,
            workflow_type=workflow_name,
        )
        return {
            "ok": True, "tool": name,
            "summary": f"已提交 {workflow_name} 生成任务，task_id={task_id}",
            "payload": {"task_id": task_id, "workflow_type": workflow_name},
        }
    except Exception as exc:
        return {"ok": False, "tool": name, "error": str(exc), "summary": f"任务提交失败: {exc}"}
```

**Step 4.3**：在 `_dispatch()` 中将 stub 替换为 real：

```python
# 将
if name in _TOOL_TO_WORKFLOW:
    return _exec_generate_stub(name, args, ctx)
# 替换为
if name in _TOOL_TO_WORKFLOW:
    return _exec_generate_real(name, args, ctx)
```

---

## Phase 5：端到端验证（1天）

### 验证清单 A：Phase 1+2（agent 对话流程，stub 阶段）

```bash
# 启动服务
cd Edu_AI/api/src && python -m uvicorn main:app --port 8000

# 测试 1：普通对话（不调工具）
# 问：量子纠缠是什么意思
# 期望：delta 流式回答，日志无 "[AGENT] tool_calls"，TTFT < 1s

# 测试 2：追问然后大纲
# 问：帮我写报告
# 期望：agent 追问主题（delta 事件），不调 draft_outline
# 回答主题后：agent 调用 draft_outline，然后展示大纲（delta 事件）

# 测试 3：大纲确认后生成（stub）
# 用户说"可以"后：agent 调用 generate_report
# 期望：task_submitted 事件，task_id 以 "stub-" 开头

# 测试 4：练习题直接生成
# 问：出 10 道 Python 基础选择题
# 期望：直接调用 generate_quiz，无 draft_outline 调用
```

### 验证清单 B：Phase 3+4（真实后台任务阶段）

```bash
# 测试 5：报告 fast-path
# 对话中确认大纲后，agent 调用 generate_report(confirmed_outline=...)
# 期望：task_submitted 事件返回真实 task_id，不再以 "stub-" 开头
# 期望：任务完成结果 trace.agent_fast_path=true，不出现 ask_critical_gap / soft_confirm 阶段

# 测试 6：PPT fast-path
# 确认大纲后调用 generate_ppt
# 期望：Markdown 大纲正确解析为 PptOutline
# 期望：workflow.phase 进入 submitting_ppt_job 或 polling_ppt_job

# 测试 7：Quiz fast-path
# 调用 generate_quiz
# 期望：直接生成 quiz artifact，trace.agent_fast_path=true

# 测试 8：按钮路径不受影响
# 设置 action_hint="generate.report"
# 期望：不进入 agent，走原有完整 workflow 路径
```

### 验证清单 C：性能基准

```bash
# TTFT 对比（与 P0-B 基准相比）
# 普通对话：TTFT 差异 < 100ms
# 资源生成首 delta（追问）：TTFT < 1.5s（agent planning call）

# 工具调用次数上限验证
# 构造需要 5 次工具调用的场景
# 期望：REACT_MAX_STEPS=4 生效，第 5 次调用被拒绝，fallback 到 fast_runtime
```

### Grep 检查：反模式检测

```bash
# 确认没有遗漏的 stub
rg "_exec_generate_stub|stub-" Edu_AI/api/src/app/chat/runtime/
# Phase 4 完成后应无输出；若只在测试 fixture 中出现，测试文件必须明确标注为 Phase 1 stub test

# 确认 fast-path 注入点
rg "_agent_params|_agent_fast_path|agent_fast_path" Edu_AI/api/src/app/chat/workflows/
# 应出现在 4 个 workflow runtime 文件中

# 确认 route_decision 扩展
rg '"agent"|def agent' Edu_AI/api/src/app/chat/domain/route_decision.py
```

### 20 条规格用例回归矩阵

| 编号 | 覆盖点 | 验证方式 | 通过标准 |
|------|--------|----------|----------|
| 1~4 | 普通问答不调工具 | SSE 手测 + 日志 | 只有 `delta/result`，无 `tool_call` |
| 5~7 | 信息不足先追问 | SSE 手测 | 第一轮不调工具；补齐主题后才 `draft_outline` |
| 8~9 | 信息充足先大纲 | SSE 手测 | 先 `draft_outline`，用户确认后才 `generate_*` |
| 10,17 | 大纲修改 | 对话手测 | 修改后重新展示大纲；确认后传入最终 `confirmed_outline` |
| 11~12 | Quiz 直接生成 | SSE 手测 | 直接 `generate_quiz`，无 `draft_outline` |
| 13,15,16 | RAG 门控 | capability 开关测试 | `allow_rag=false` 时模型不可见 `rag_search`；true 时可调用 |
| 14 | Web 门控 | capability 开关测试 | `allow_web=false` 时模型不可见 `web_search`；true 时可调用 |
| 18 | “继续”上下文 | 带历史 conversation 测试 | agent 可基于 snapshot/history 回复或 fallback，不误触发新任务 |
| 19 | 按钮路径 | action_hint 请求 | 直接进入 workflow，日志无 ReActAgent run |
| 20 | 多轮后生成 | 真实 E2E | 后台任务收到 `_agent_params.confirmed_outline` |
```

---

## 禁止事项（Anti-Patterns）

| 禁止 | 原因 |
|------|------|
| 在 `stream_chat_with_tools()` 中使用 `choice[0]["message"]["tool_calls"]` | 这是非流式响应格式，流式中在 delta 里 |
| `submit_workflow_task()` 不传 `decision` 参数 | 该参数为必填，workflow.run() 依赖它 |
| PPT fast-path 直接把 Markdown 字符串传给 PptOutline | 需要先调用 `_parse_markdown_to_ppt_outline()` 解析 |
| `agent_tools.py` 中 import 循环（在模块级别 import workflow） | 所有 workflow import 放在函数内部 |
| 修改 `gather_context` 的公共构建逻辑 | 风险太高，改用 `_agent_params` 属性注入代替 |
| 删除或修改 `route_rules.py` | 该文件是 fallback 路径，保留不动 |

---

## 文件改动总览

| 文件 | 操作 | Phase |
|------|------|-------|
| `app/chat/domain/route_decision.py` | 修改（加 "agent"） | 1 |
| `app/chat/model_gateway.py` | 修改（加 stream_chat_with_tools） | 1 |
| `app/chat/runtime/agent_tools.py` | **新建** | 1 |
| `app/chat/runtime/react_agent.py` | **新建** | 2 |
| `app/chat/orchestrator/main_orchestrator.py` | 修改（加 agent 路径） | 2 |
| DI 工厂 / routes_v2.py | 修改（注入 react_agent） | 2 |
| `app/chat/workflows/report/runtime.py` | 修改（加 fast-path） | 3 |
| `app/chat/workflows/ppt/runtime.py` | 修改（加 fast-path + Markdown 解析） | 3 |
| `app/chat/workflows/lesson_plan/runtime.py` | 修改（加 fast-path） | 3 |
| `app/chat/workflows/quiz/runtime.py` | 修改（加 fast-path） | 3 |
| `app/chat/runtime/agent_tools.py` | 修改（stub → real） | 4 |
| `core/config.py` | **已完成**（已添加 REACT_* 配置） | 已完成 |
