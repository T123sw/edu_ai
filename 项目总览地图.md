# edu_ai · 项目总览地图

> **这份文档的作用**：每次启动 Claude / 接手工作时**先读这一份**，快速建立全貌，不必重新通读全仓库。
> 只做「地图」——指路，不展开细节。细节去对应的专项文档 / 代码里看。
> 维护约定：目录结构、决策、阶段状态发生**结构性变化**时更新本文；日常改代码不用改这里。
> 最近更新：2026-07-01

---

## 1. 这个项目是什么（目标）

**edu_ai = 一个 AI 驱动的教学内容生产 + 授课平台。** 教师给需求/教材，系统自动产出**结构化课件**，并以三种平行形态交付：

1. **交互课堂**：浏览器里播放的动画幻灯片 + 聚焦高亮 + TTS 语音旁白（**放弃数字人/唇形同步**）。
2. **PPTX**：可下载的课件文件。
3. **MP4 讲解视频**：自动录制/渲染的成片（先 A 后 B，见 §5）。

核心知识源是 edu_ai 自己的 **RAG 检索 / 教材正文 / 知识图谱**（计算思维、数据结构、操作系统、计算机网络、数据库、计算机组成等课程）。

**当前最大的架构决策**：不再自造课件/视频链路，改为**复用开源项目 OpenMAIC**（MIT）——用它的课件数据契约（Stage/Scene/Action/Slide DSL）+ 前端播放/导出包 + Node sidecar 后端能力；edu_ai 负责编排、存储、权限、知识源。**放弃并将下线** html2ppt、AI_Lecturer（数字人）、teaching_video_bridge。

---

## 2. 当前状态（2026-07-01）

- **OpenMAIC 复用方案已拍板并成文**，进入分阶段实施。总纲见 `docs/OpenMAIC复用_实施总纲_2026-06-30.md`（照着干看这份）。
- **Phase 0 已验证通过**（2026-06-30，本机）：sidecar 能起、`/api/parse-pdf`（MinerU Cloud）端到端解析成功。跑法与三个坑记录在用户记忆 `openmaic-sidecar-run-notes.md`。
- **Phase 1 已完成并验收通过**（2026-07-01，`cd85567`）：PDF 解析改为 Python 直连 MinerU Cloud provider；图片存储链路收口（`linked_images` 端到端 + guarded 媒体 URL 重写 + `rag_main` `BASE_DIR` 修正对齐 host `src`）；**删净本地解析降级**（pymupdf/fitz + docling 全移除，`EduAgent` 本地 PDF 解析改存根），PDF 转为 MinerU-only、失败即失败。专项测试 `tests/chat/test_rag_v2_runtime_import.py` 44/44 通过；全量 `tests/chat` 无新增回归（既有失败 30→27，均在 report/ppt/reply/startup 无关域）。详见 SPEC-03 / ACC-03。
- **Phase 2（课件生成 MVP）已完成**（2026-07-24，`56350cc`→`b73224c`，P2-1~P2-5 全部收官）：P2-1 sidecar vendor 接入 `edu_ai/openmaic-sidecar/`（方案A，`/api/health` 已复验）→ P2-2 researchContext 注入补丁（SPEC-04 §4 三处，`docs/spec/patches/001`）→ P2-3 `OpenMaicClient`（`app/integrations/openmaic/`）→ P2-4 job 表衔接（`app/services/job_store.py`+`classroom_job_service.py`，"完成语义差异"闸门）→ P2-5 `classroom_service` 编排（RAG Top-K + web 合并注入、SPEC-02 §6 校验、落库复用 `CourseStorageManager.save_generated_material`）。全量回归 866 passed，0 新增回归。详见 SPEC-04/07/05/02 与用户记忆 `edu-ai-phase2-classroom-mvp.md`（含 RAG 检索"course 级 scoping 不存在，用课程 KB 文档桥接"的重要现实落差记录）。
- **旧链路仍在仓库里**（html2ppt / AI_Lecturer 未删），按"先影子验证再删"的策略，等替代链路验收后再下线（Phase 6）。
- 主应用（Edu_AI 前后端）是**已跑通的现网系统**（单机 Linux：nginx + systemd/uvicorn），当前承载聊天、深度搜索、课程管理、知识图谱、旧版 PPT/视频。

**阶段路线**（`mineru → ppt → video` 三段式；细节见总纲 §7）：

| Phase | 目标 | 状态 |
| --- | --- | --- |
| 0 打底 | sidecar 可行性 + 最小 .env | ✅ 已验证 |
| 1 解析替换 | Python 直连 MinerU Cloud provider 替换本地 mineru；图片存储收口；删 pymupdf/docling 本地解析降级（PDF 转为 MinerU-only，失败即失败） | ✅ 已完成（2026-07-01，`cd85567`） |
| 2 课件生成 | 调 `generate-classroom`，注入 researchContext，落库 Stage/Scene | ✅ 已完成（2026-07-24，`b73224c`） |
| 3 交互课堂 | 前端引 `@openmaic/dsl+renderer`；LessonTimeline 编译器 + ClockSource 播放器 + 三接缝（聚焦/语音在这里） | ⏳ 关键地基 |
| 4 导出 PPTX | pptxgenjs | ⏳ |
| 5 视频 A | 无头回放录屏 + TTS mux + SRT | ⏳ |
| 6 下线旧模块 | 删 html2ppt/AI_Lecturer/bridge | ⏳ |
| B 择机 | 确定性逐帧渲染（Remotion 式） | 远期 |

---

## 3. 顶层目录地图

```
edu_ai/
├── 项目总览地图.md          ← 本文（启动先读）
├── AGENTS.md                技能系统配置（openskills）；非业务
├── DEPENDENCIES.md          依赖清单
├── docs/                    ★ 所有设计/方案/实施文档（见 §6）
├── Edu_AI/                  ★★ 主应用：React 前端 + FastAPI 后端（见 §4）
│   ├── src/                   前端源码
│   └── api/Edu_AI 或 api/src/ 后端源码
├── EduAgent/               独立的 DeepSearch/文档摘要 Agent 服务（早期，部分能力已并入主应用）
├── automation_spider/      爬虫模块：为知识图谱/知识库抓取材料
├── infra/searxng/          SearxNG 自建搜索（供 deepsearch 用）
├── scripts/                运维/诊断/一次性脚本（install-all、mineru、e2e 测试等）
└── openmaic-sidecar/       OpenMAIC 整体 vendor（方案 A，已接入），对外 /api/generate-classroom 等；
                            Python 侧 client/service 尚未接（见 SPEC-04/07 进度）
```

---

## 4. 主应用 Edu_AI 内部地图

### 4.1 前端 `Edu_AI/src/`（React + Vite + TS + Tailwind + Zustand）

| 路径 | 放什么 |
| --- | --- |
| `main.tsx` / `routes/` / `layout/` | 入口、路由、布局骨架 |
| `pages/teacher/*` | 教师端页面：AI 工作台、课程/资源/知识图谱、教师工具 |
| `pages/student/*` | 学生端页面：课程学习、AI 学生工作室、学习记录 |
| `pages/*`（顶层） | 通用页：Chat、DeepSearch、KnowledgeBase、DataPipeline、Login、Welcome、UserCenter |
| `services/` | 调后端的 API 客户端：`auth / chat / deepsearch / rag / knowledgeBase / pipeline / teacher/*` |
| `store/` | Zustand 状态：`course/ teacher/ useUiStore` |
| `components/{teacher,student,shared}` | 复用组件 |
| `stitch/` | 相对独立的一套 UI（api/components/hooks/pages + wordExport）——注意与主 src 并存 |

> Phase 3 时前端会新增：引入 `@openmaic/dsl` + `renderer`，做 SlideCanvas / PlaybackEngine / 聚焦 overlay / LessonTimeline 编译器 / ClockSource 播放器。

### 4.2 后端 `Edu_AI/api/src/app/`（FastAPI，入口 `app.main:app`）

顶层 `.py`（按域）：`main / bootstrap / auth / dependencies` 基础设施；`courses / deepsearch* / textbook_knowledge_graph / knowledge_graph_hours` 业务；`ai_lecture_sessions / teaching_video_bridge / video_routes` 授课/视频（**bridge 属旧链路，待删**）。

子模块：

| 目录 | 职责 |
| --- | --- |
| `app/chat/` | ★ **核心：ReAct Agent 系统**（P4A 重构）。含 `agents/ application/ domain/ orchestrator/ engines/ runtime/ memory/ retrieval/ tools/ tasks/ persistence/ workflows/`。是聊天+内容生成的大脑。 |
| `app/chat/workflows/` | 具体工作流：`ppt/`（html2ppt，**旧，待删**）、`lesson_plan/`、`quiz/`、`report/` |
| `app/services/` | 业务服务层：`chat / course / deepsearch / teacher / video`_service |
| `app/api/` | 路由：`courses / teacher / deepsearch / video / searched_images / health / chat_legacy` |
| `app/pipeline/` | 数据管道（models/routes/storage/tasks）——统一异步任务模型 |
| `app/speech/` | 语音：`transcribe`（转写）+ routes |
| `app/report_service/ blog_agent/ integrations/ schemas/ core/ domain/` | 报告生成 / 博客 Agent / 外部集成 / 数据模型 / 核心 |
| `app/modules/AI_Lecturer/` | ★ **数字人整套（Wav2Lip / LiveTalking / offline_video_maker），已决策放弃，待删/归档** |
| `app/legacy/` | 旧处理器归档 |
| `api/course_data/courses/` | 各课程的知识库 / 生成材料落盘目录 |

---

## 5. 视频导出：A→B 演进（关键设计，别踩坑）

- **唯一共享地基 = `LessonTimeline` 数据契约**（scene/action 编译成显式多轨时间线，每 clip 有 `startMs/durationMs/track/durationSource`）。
- **A（先做，MVP）**：无头 Chromium 干净回放已生成课件 + 按 scene 录屏；音频**拼不录**（预生成 TTS 按时间戳 mux）；产出 = 视频 + 一份 `durationSource='measured'` 的完整 timeline。
- **B（后做）**：确定性逐帧渲染（Remotion 式），吃 A 产出的 timeline。
- **A 阶段必须留好的三个接缝**（否则 B 要重写）：① `ClockSource` 注入（播放器只认 `currentTimeMs()`）；② 效果组件加可选 `localTimeMs` prop（先加不接线）；③ 视频元素始终走 `renderVideo` 插槽。
- 详见 `docs/课件视频_统一时间线契约与AB演进预留设计_2026-06-30.md`（做 Phase 3/5/B 必读）。

---

## 6. 文档索引 `docs/`（按用途）

**OpenMAIC 复用（当前主线）**
- `spec/`（★ 规格层，2026-07-01 起）— **落地级 spec：每个接口/字段/文件怎么接**。先读 `spec/README.md`（总/索引），再看 `SPEC-01~08`（分：sidecar/数据契约/parse-pdf/generate-classroom/任务协议/BYOK/Python客户端/前端集成）。总纲说「搬什么」，spec 说「一行行怎么接」。
- `acceptance/`（★ 验收层，2026-07-01 起）— **每份 spec 对应一份 ACC：实现什么/验收标准/怎么测**。先读 `acceptance/README.md`，`ACC-01~08` 与 `SPEC-01~08` 一一对应。**地图↔spec↔验收 三层互有指针**：spec/ACC 顶部回指地图，ACC 指向 spec，spec 指向 ACC。
- `OpenMAIC复用_实施总纲_2026-06-30.md` — **主实施文档，搬什么/怎么搬/接口/删替/路线**。照着干看这份。
- `OpenMAIC_对比分析与替换方案_v2_2026-06-30.md` — 为什么换、对比依据。对外解释看这份。
- `OpenMAIC_对比分析与借鉴建议_2026-06-30.md` — v1 对比（历史）。
- `课件视频_统一时间线契约与AB演进预留设计_2026-06-30.md` — 视频 A→B 数据结构与代码接缝。

**Agent 架构（chat 模块背景）**
- `agent_architecture_design_2026-05-25.md`、`agent_flow_issues_and_redesign_2026-05-25.md`、`agent_memory_system_design_2026-05-25.md`、`agent架构设计`、`agent生成工厂.md`
- `P4A_ReAct_Agent化_设计规格_2026-05-18.md`、`P4A_实施计划_2026-05-18.md` — chat 的 ReAct 重构

**配图能力（已实现）**
- `Phase6A_image_search_实施计划_2026-06-11.md`、`Phase6A2_image_localization_实施计划_2026-06-29.md`

**路线图 / 综述 / 运维**
- `系统现状与优化路线图_2026-04-28.md`、`系统优化路线图_2026-05-14.md`、`功能重建.md`、`tvir总结`
- `换机器部署清单.md` — 跨机器部署（OpenMAIC 化后：去 GPU，加 Node sidecar + 在线 API key）
- `计算思维知识图谱.md` — 知识图谱内容

---

## 7. 怎么跑起来（速查）

- **Conda 环境**：主应用 `conda activate edu_ai`；OpenMAIC sidecar `conda activate openmaic`（node22 + pnpm10）。
- **后端**：`cd Edu_AI/api/Edu_AI` → `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`
- **前端**：`cd Edu_AI` → `npm run dev`（构建：`.env` 配 `VITE_API_BASE_URL` 后 `npm run build`）
- **现网访问**：`http://192.168.1.51`（nginx 静态 + systemd/uvicorn）；默认账号 `teacher / teacher123`
- **sidecar 本机坑**（Phase 0 记录）：① pnpm postinstall 需 `rm`，把 `C:\Program Files\Git\usr\bin` 加进 PATH；② 本机代理 `127.0.0.1:7897` 拦 localhost，测试加 `--noproxy`；③ DeepSeek 模型名 `deepseek-v4-pro`。

---

## 8. 术语 / 心智模型速记

- **三段式**：`mineru`(解析=Phase1) → `ppt`(结构化数据 Phase2 + 交互课堂 Phase3 + 导出 Phase4) → `video`(A→B, Phase5+)。
- **聚焦和语音讲解不是独立阶段**，它们是时间线上的 spotlight/speech 动作，**随 Phase 3 交互课堂一起到来**。
- **数据契约**：`Stage → Scene[] → (Slide DSL + Action[])`，直接用上游 `@openmaic/dsl`，不自造。
- **统一异步任务协议**：`{jobId, pollUrl, pollIntervalMs}` + `{step, progress, message}`，全系统长任务统一。
- **内容来源注入**：edu_ai 把 RAG/教材/知识图谱片段拼成 `researchContext` 注入 OpenMAIC 生成流水线 = "骨架用它的，知识源用自己的"。
- **id 不可变**：scene/action/element/clip id 稳定，支撑增量重渲、分段录制、聚焦寻址、局部重生成。
- **BYOK + 服务端托管混合 key 模型**：保留 OpenMAIC 前端配置页时，必须连同其安全边界（托管优先 + 忽略客户端 key + SSRF 校验）一起保留。
```
